# Decisions log

Append one line per decision. Never edit or delete existing lines.
Format: YYYY-MM-DD | initials | decision

2026-09-13 | PS | Spike scope frozen: prove model-response interception and injection only.
2026-09-13 | PS | Bedrock preflight passed. ap-south-1 requires inference profile ID apac.amazon.nova-lite-v1:0; bare model ID rejected with ValidationException.
2026-09-13 | PS | Observed Nova at temperature 0 return "Preflight OK" when asked for exactly "preflight ok". Confirms hosted inference is not reproducible. Injection is the only defensible determinism claim.
2026-09-13 | CC | Spike PASSED via RULE B: BedrockModel subclass overrides stream() to yield recorded stream events, tools replayed via public BeforeToolCallEvent.selected_tool hook; replay hash matched recording with 0 Bedrock HTTP sends and 0 tool-body executions (no hook can write a model response, so RULE A was not available).
2026-09-13 | CC | Gate replay added (spike/gate.py): tools injected from golden trace by name+args, model LIVE with changed prompt/model ID. Verified: unchanged config still FAILs on DIFFERENT_ANSWER (Nova non-determinism, not a bug); prompt bias toward skipping lookup gives ATTRIBUTABLE DIFFERENT_TOOL at step 1 with zero unrecorded calls (order-independent matching absorbed the reorder, so --strict had nothing to halt on); tone-only change gives ATTRIBUTABLE DIFFERENT_ANSWER only, no tool-level divergence; a genuinely novel tool call correctly halts under --strict and is UNRECORDED/UNATTRIBUTED otherwise.
2026-09-13 | CC | Gate replay verdict split into PASS/WARN/FAIL: trajectory divergence (DIFFERENT_TOOL/ARGS/EXTRA_STEP/MISSING_STEP/UNRECORDED) is FAIL exit 1; a matched trajectory with only DIFFERENT_ANSWER is WARN exit 0 by default (--fail-on-answer promotes it to FAIL), since Nova wording is not reproducible even with nothing changed. Re-verified: 3x case A all WARN/exit 0; case B/D FAIL DIFFERENT_TOOL step 1; case C WARN; unrecorded-call extras unchanged (FAIL/UNRECORDED and strict halt).
2026-09-13 | CC | Phase 1 probe: Nova via Strands returned parseable JSON with exact expected keys 5/5 runs when asked to end response with a fixed-shape JSON object. Mutation replay added (--mutate TOOL:JSON, repeatable, shallow-merged into the recorded tool result before injection, validated against golden tool names before anything runs); step table marks the mutated step MUTATED. Answer-tier removed: verdict is trajectory-only PASS/FAIL, final-answer text is informational (matched boolean + generic JSON diff if both parse), --fail-on-answer opts back into strict text matching. Re-verified A/B/C/A-2002 unchanged in outcome; M1 (refund_eligible true->false) correctly produced MISSING_STEP/ATTRIBUTABLE (agent skipped the refund); M2 cosmetic mutation PASSed; M3 confirmed prompt+mutation both named in the report.
2026-09-13 | CC | Toy agent replaced for full-replay proof purposes with spike/audit_agent.py (new file, spike/agent.py untouched): dependency security auditor hitting real pypi.org/api.osv.dev, requests library (already transitive via strands-agents-tools, no new dependency). Golden trace traces/audit-001.json: 11 events, 66.2KB. Full replay PASSed with 0 real tool executions and 0 non-loopback socket ops -- the first time that network-zero count is proven against tools that would genuinely reach the internet. Gate replay against this agent surfaced two real findings unrelated to any prompt change: the model checks independent packages in a different order each run, which the positional comparator reports as false-positive DIFFERENT_ARGS/DIFFERENT_TOOL; and it intermittently mangled a long backslash-heavy Windows path argument. Required a minimal, flagged gate.py change: --agent-module for dynamic agent selection, and _apply_mutation now detects JSON tool-output text (audit_agent) vs the toy agent's key=value text before merging, since --mutate could not otherwise touch a JSON-shaped tool result.
2026-09-14 | CC | Comparator rewritten from positional to dependency-aware (spike/gate_compare.py): happens-before edges from substring-matching argument values against prior tool outputs; PASS requires same multiset of calls AND edge order preserved both ways. Retired DIFFERENT_TOOL/DIFFERENT_ARGS/EXTRA_STEP (no longer meaningful without positions); added ORDER_VIOLATION (edge broken, unit-verified both directions) and UNSOURCED_ARGUMENT (unrecorded call whose args trace to no prior output or prompt -- takes precedence over UNRECORDED). Toy agent: A/M2 PASS, B/M1 FAIL correctly (B mostly via MISSING_STEP -- the biased agent omits lookup_order rather than reversing it; ORDER_VIOLATION confirmed working via direct synthetic test). Audit agent: full replay still PASS/0-network; gate-replay-unchanged FAILed 5/5 for two real, non-comparator-bug reasons -- a false happens-before edge from substring name overlap ("strands-agents" is literally a substring of "strands-agents-tools"), and boto3 being unpinned in requirements.txt so the model fabricates a plausible version every run (even golden's own recording does this) -- both explained in chat, neither papered over. Also demonstrated a real permissiveness gap: a candidate that reads refund_eligible=false but calls issue_refund anyway (order_id never encodes that field) gets a clean PASS -- the comparator verifies data availability, not decision correctness. Fixed audit_agent target to a file:// URL (was a backslash Windows path Nova intermittently mangled) and added alphabetical-order processing to its system prompt (demo-stability only, not a fix).
2026-09-14 | CC | Three targeted fixes: (1) edge detection changed from substring to exact-token matching (regex split, hyphens not a separator) -- fixes the strands-agents/strands-agents-tools false edge, verified ORDER_VIOLATION still fires on the toy agent's synthetic reversed-order trace and PASS still holds for correct order; (2) boto3 pinned to 1.43.93 (matches pip freeze) and traces/audit-001.json re-recorded -- golden is now internally consistent, every check_vulnerabilities version traces to get_package_info or the manifest; (3) gate.py always prints golden vs candidate final-answer text (informational, does not affect verdict) -- the mutation verdict flip SAFE TO DEPLOY -> BLOCKED is now directly readable in the report. Toy agent (A/B/M1/M2) all verified correct. Audit agent full replay PASS/0-network. Gate-replay-unchanged still FAILed 5/5 for a third, distinct, real cause found after fixes 1+2 landed: golden's own trajectory calls get_package_info(boto3) and get_package_info(strands-agents) AFTER already using their versions (redundant/habitual confirmation), and check_vulnerabilities echoes its own name/version arguments back in its output, so golden's own idiosyncratic ordering is read as a required edge that a more natural verify-before-check ordering violates. Not fixed per instruction (no third comparator redesign); reported to the user for a decision. Added docs/LIMITATIONS.md.
2026-09-14 | CC | Final comparator fix: a token echoed from a call's own input arguments is excluded from that call's output-as-source only if the token ALSO already appeared in an earlier call's output this run; a token echoed from input that has no earlier occurrence is the call's own primary subject (e.g. an id the caller supplies) and still anchors an edge. Fixes the audit agent's reversed false edges (check_vulnerabilities echoing name/version, wrongly appearing to precede get_package_info) while preserving the toy agent's lookup_order->issue_refund edge (order_id has no earlier occurrence, since lookup_order runs first) -- verified via both the synthetic reversed-order (FAIL/ORDER_VIOLATION) and correct-order (PASS) traces. Golden's remaining edge set (7 edges) confirmed all genuine by inspection. Re-verified toy agent A/B/M1/M2 all correct. Audit agent: full replay PASS/0-network; gate-replay-unchanged now 4/5 PASS, 1/5 FAIL -- the one failure is real live-model variance (an unprompted extra tool call), not a comparator artifact, documented in docs/LIMITATIONS.md with the measured ~20% rate; per instruction, not treated as something to fix further. Loosened-prompt run now shows the real regression cleanly: MISSING_STEP, fully ATTRIBUTABLE, candidate's own text explains it skipped strands-agents-tools by "judgement." Mutation clean->CRITICAL: trajectory PASS (actions identical, only injected data differed), verdict flip SAFE TO DEPLOY -> BLOCKED directly readable in the report. Comparator work stops here per instruction; moving to the storage layer next.
2026-09-14 | CC | AWS storage backend added: spike/storage.py (TraceStorage ABC, LocalTraceStorage unchanged-behavior wrapper, AwsTraceStorage). DynamoDB table "agent-replay" (pk run_id, sk sequence_number, on-demand) holds only small per-event fields; S3 (CloudFormation-auto-named bucket, no account ID in the name, 7-day expiry lifecycle rule) holds every event payload content-addressed by SHA-256, reusing the hashes replay already computes. Deployed via infra/template.yaml as stack agent-replay-dev in ap-south-1 (required the user to grant cloudformation:* to CLI_Agent_Replay, which had none). Load phase (S3/DynamoDB fetch) is structurally separated from run phase in replay.py/replay_audit.py, with counters snapshotted at the boundary; verified full replay from --storage aws still PASSes with 0 real executions and 0 non-loopback sockets during the run phase specifically (load phase alone used 6, expected and excluded by design). Recording writes are buffered in memory and flushed after the run completes (not blocking the agent loop): measured latency 1.5ms local vs 3252.4ms AWS for an 11-12 event audit trace. Dedup measured by recording the same audit run twice: 1/22 payloads deduped (read_manifest's input URL, the only payload structurally guaranteed identical across two live runs -- model text is never identical and tool outputs vary run to run). --storage local confirmed unchanged on record/replay/gate. Stack left running per instruction; teardown requires emptying the S3 bucket before stack delete (CloudFormation cannot delete a non-empty bucket).
2026-09-14 | CC | Two fixes + perf change to AWS storage. (1) S3 keys now golden/<hash>.json vs runs/<hash>.json; lifecycle rule ExpireNonGoldenTraces scoped to Prefix runs/ only, so a golden trace never expires while DynamoDB still lists it. record_audit.py --golden sets run_kind=golden. Deleted 45 orphaned objects and their 2 stale DynamoDB run items from the old flat events/ key layout before the stack update, per instruction not to leave unowned objects. (2) Metadata item now stores prompt_hash/final_answer_hash only; prompt and final_answer are content-addressed in S3 like every other payload, fetched during the (already network-bound, proof-exempt) load phase. (3) S3 head+put calls parallelized via a bounded ThreadPoolExecutor(8); DynamoDB batch_writer left as-is. Verified: golden lands under golden/, normal run under runs/; full replay from AWS golden PASSes with 0 real executions and 0 non-loopback sockets during the run phase; gate replay from AWS golden PASSes; --storage local unchanged on record/replay/gate. Write latency ~2030-2037ms for an 11-event trace, down from 3252ms for a 12-event trace (~1.5x per-event improvement). Added a Scaling boundaries section to docs/LIMITATIONS.md documenting (not fixing) unpaginated query, no secondary index, no optimistic locking, and per-init describe_stacks cost, plus the dedup-is-a-measurement finding.
2026-09-14 | CC | Packaged as installable agent-replay (pip install -e ., pyproject.toml; spike/ left completely unmodified, agent_replay/paths.py puts it on sys.path exactly as running "python spike/gate.py" already does): agent-replay init/record/test/replay/gate, contract derivation as exact set logic (requires/permits/order reusing gate_compare._build_edges unchanged, aggregated to tool-name granularity) written to agent-replay/contracts/<scenario>.yaml in a hand-formatted tool(arg=value) call-signature format (not yaml.dump) so a contract diff reads in seconds, plus deterministic forbids rules (when/count/count-per-arg, no LLM judgement) evaluated against the exact tool results injected during a run. Verified: vulnerable-dependency (audit_agent, 3 runs) derived requires=7/permits=0/order=3, then agent-replay test PASSed 5/5 exit 0 against live Bedrock; adding "issue_refund when lookup_order.refund_eligible == false" to the toy agents customer-refund contract correctly FAILed as FORBIDDEN (not MISSING_STEP) once the agent was made to call issue_refund anyway, and still PASSed unmutated; loosening the audit agents prompt to skip two packages correctly FAILed on MISSING_STEP with a 6-line contract diff; --json and --format junit both emit valid parseable output; confirmed zero boto3.client() calls and zero real tool-body executions during --storage local runs (only Bedrock + PyPI/OSV network activity, from the model and the reference recordings own tool bodies at record time). New dependency: PyYAML (already installed transitively via strands-agents-tools) for scenarios.yaml and contracts/*.yaml; arg parsing stayed stdlib argparse, no new dependency there.
2026-09-14 | CC | Phase 1 portability: added agent_replay/portable.py (collapse-at-capture, expand-at-use, collapse-at-comparison) so a scenario prompt is authored with a {repo} token, expanded to a real absolute path only for the live agent call and for widening a portable reference recording back into gate.py GateToolTap (unchanged, needs real args to exact-match), and collapsed back to {repo} before anything is persisted or before a live candidates args are checked against contract keys. Migrated the 5 existing on-disk traces and re-derived both contracts in place (pure data transform, zero Bedrock/PyPI calls, no re-recording) -- this incidentally exposed and required restoring a real bug: contract.derive() always resets forbids to [], silently destroying the hand-authored customer-refund forbids rule from the prior session; restored it, left the underlying bug undocumented-but-real for the final report since fixing derive-vs-human-edits was out of scope for this phase. Verified end-to-end with a real Bedrock call: copied the repo (agent_replay/, spike/, agent-replay/, traces/, pyproject.toml) to a fresh checkout on a different drive letter, fresh venv, fresh pip install -e ., and agent-replay test vulnerable-dependency PASSed against the copied traces, exit 0. Confirmed --replay (byte-identical mode) still works on the SAME machine after migration by expanding the whole loaded trace back to absolute at replay time, but explicitly does NOT claim --replay is portable across machines (the recorded model conversation itself is never rewritten -- see LIMITATIONS.md). Grepped the full repo for drive letters/absolute paths: none remain outside illustrative comments/docstrings and .venv.
2026-09-14 | CC | Phase 2: recorded vulnerable-dependency with --runs 5 (the one authorized recording this session) to test whether the multi-run envelope earns its place. permits stayed empty at N=5, identical in shape to N=3 -- requires=7/order=3 both times. Root cause: audit_agents system prompt and the pinned, unvarying manifest leave the model zero genuine discretion (every pinned package always gets checked with the same version), so N runs currently buy confidence that requires is stable, not visible new information; historically the order fields cross-run intersection DID matter (per this logs prior single-golden-trace comparator bugs from one runs idiosyncratic ordering). Proposed but did NOT record (per instruction to stop and wait) a mixed-manifest scenario (2 pinned + 2 unpinned packages, unchanged agent/prompt) designed to expose real run-to-run variance in permits, since the existing scenario structurally cannot. Reported to the user for a decision before recording anything further.
2026-09-14 | CC | Fixed the forbids-clobber bug: contract.derive() now takes existing_forbids and carries it forward instead of resetting to [] on every re-derivation; added contract.forbids_warnings() to flag (not drop) a preserved rule whose tool no longer appears in the new recording. cmd_record now reads the on-disk contracts forbids before overwriting it. Verified: added a rule, re-derived vulnerable-dependency from its 5 existing traces with no re-recording, rule survived; added a rule naming a nonexistent ghost_tool, re-derived again, rule was KEPT and the expected warning fired naming ghost_tool; both test rules removed afterward. Phase 2 resolved per instruction: predicted requests/pyyaml permits behavior before recording a new vulnerable-dependency-mixed scenario (audit_agent unchanged, one new fixture with 2 pinned + 2 unpinned packages, 5 runs, the second and final authorized recording); permits populated (10 entries) validating the feature, but two specific predictions were wrong (pyyaml landed in requires at 5/5 -- MORE consistent than requests, the opposite ranking predicted; strands-agents, a PINNED package, unexpectedly landed in permits at 3-4/5 rather than requires, apparently ordinary live-model variance rather than genuine discretion) -- reported plainly rather than reinterpreted. Decision applied: keep --runs N and the README claim, documented with this real (populated, imperfect) example instead of the all-empty one, explicitly not described as a headline feature; also surfaced and documented two unplanned findings (exact-match keying treats "pyyaml" and "PyYAML" as different entries; the model sometimes emits the literal string "latest" as a version value) and one real instability (a fresh agent-replay test against the mixed contract can still FAIL on a pyyaml version string none of the 5 references produced -- an honest limitation of finite N on an unbounded-variation input, not a bug).
2026-09-14 | CC | Phase 3: added 4 docs/LIMITATIONS.md sections -- order-edge granularity is tool-name level not per-instance (concrete uncaught-regression example given: check_vulnerabilities(boto3) with no prior get_package_info(boto3) passes order if ANY get_package_info happened earlier this run, since evaluate.py order_seen tracks tool names only); the callsig tool(arg=value) formats numeric-string coercion caveat plus a newly-observed related gap (exact-match keying treats pyyaml vs PyYAML as different entries, and a live model was observed emitting the literal string "latest" as a version value); editable-install-only (pip install agent-replay from PyPI does not work, agent_replay/paths.py requires its own parent directory to be the actual repo checkout); and two carried-forward findings from phases 1-2 -- replay byte-identical mode is verified same-machine only, never claimed cross-machine, and a fresh CI git clone has zero local trace files since traces/ is gitignored, so CI must use --storage aws. Confirmed README never claims a PyPI install works.
2026-09-14 | CC | Phase 4: added .github/workflows/agent-replay.yml (pull_request not pull_request_target, reasoning documented inline: scenarios.yaml names an arbitrary agent module this workflow imports and runs, so a fork PR must not get this workflows AWS trust), .github/scripts/render_pr_comment.py (standalone, reads agent-replay test --json output, renders scenario-first PASS/FAIL markdown with a plain-English divergence sentence per cause, counters, attribution, a dashboard link marked not-live-yet) and post_pr_comment.sh (finds and PATCHes the existing marker comment via gh api instead of posting new ones), and infra/github-oidc.yaml (a SEPARATE template from infra/template.yaml since an AWS account permits only one OIDC provider per issuer URL -- CreateOidcProvider is a parameter precisely so an account that already trusts GitHub Actions elsewhere does not fail to deploy this). Fixed a real CLI gap this surfaced: --json and --format junit were mutually exclusive, but the workflow needs both from one live run (two separate test invocations would burn two real Bedrock calls and could disagree) -- now combinable, JUnit defaults to a real file instead of stdout when JSON already owns stdout. Added a tool_bodies counter to ContractResult (real tool executions), missing before, needed for the PR comments Counters line. Verified: ran the actual command sequence locally (pip install -e ., agent-replay test --json --format junit, the renderer) substituting --storage local for aws since no further recording was authorized -- documented as a substitution, not hidden; rendered and printed both a real PASS comment (3 scenarios) and a real FAIL comment (customer-refund FORBIDDEN, from the existing gate mutation) verbatim; added an explicit guard for the crashed-with-zero-results case, since all([]) is vacuously true; fixed a real bug the validation surfaced, bare `on:` parsing as the YAML 1.1 boolean True under plain PyYAML (GitHub Actions itself is unaffected, quoted it anyway); validated infra/github-oidc.yaml with the real, read-only aws cloudformation validate-template (not a deploy) -- confirmed syntactically valid, correctly reports CAPABILITY_NAMED_IAM required, matching the documented deploy command; confirmed JUnit output is well-formed XML. Unverified, stated plainly: real OIDC token exchange, the gh api PR-comment update path, upload-artifact, and fork-PR credential denial all require an actual GitHub Actions run against a real PR and are not simulable locally.
2026-09-14 | CC | Phase 5: added agent_replay/ci.py (reads GITHUB_SHA/HEAD_REF or REF_NAME/ACTOR, plus pr_number parsed from GITHUB_EVENT_PATH JSON when the event is pull_request; every field None when absent). Extended spike/storage.py AwsTraceStorage.save()s run_metadata item with scenario, contract_hash, branch, pr_number, triggered_by (explicit DynamoDB NULL via boto3 when absent) and commit_sha (the ONE deliberate exception -- OMITTED, not NULL, when absent, since it backs a new GSI and a GSI key attribute cannot be NULL; DynamoDBs sparse-index behavior then correctly excludes local/non-CI runs from that index rather than erroring). Restructured cli.py cmd_record into three phases (record all N runs in memory, derive+hash the contract once, THEN save each run tagged with that contract_hash) since contract_hash cannot be known until every run has been made. infra/template.yaml: added a CommitIndex GSI (commit_sha hash key, created_at range key, INCLUDE projection) -- shown as a real diff, validated syntactically via the real read-only aws cloudformation validate-template (no CAPABILITY_* needed, matches the templates own no-IAM description), update command given (aws cloudformation deploy --template-file infra/template.yaml --stack-name agent-replay-dev --region ap-south-1), NOT run. Verified metadata population by patching AwsTraceStorage S3/DynamoDB calls to in-memory fakes so the real unmodified save() code ran with zero network calls and zero cost: local (no CI vars) correctly omits commit_sha and NULLs branch/pr_number/triggered_by; a simulated pull_request CI environment correctly populates all fields including pr_number parsed from a fake event payload. Explicitly unverified and stated as such: the GSIs actual Query behavior, which requires deploying it.
2026-09-14 | CC | Phase 6 final audit: fresh-clone simulation (file copy, deliberately WITHOUT traces/ since it is gitignored and nothing this session is committed) confirmed pip install -e . works standalone, but reproduced a real, previously-only-theorized gap for real: agent-replay test vulnerable-dependency on a truly fresh checkout fails with "could not load reference recording" because the committed contract has no local trace data to inject from -- fixed by adding an explicit callout to READMEs Quickstart rather than leaving it to be discovered. Interactive agent-replay init verified end-to-end via piped stdin (zero cost, answered no to recording). Re-ran the full verification suite: agent-replay test 5/5 PASS exit 0; full replay of vulnerable-dependency--1 confirmed 0 non-loopback socket ops, 0 Bedrock HTTP sends, 0 real tool executions during the run phase, answer sha matched; the customer-refund forbids rule fired FORBIDDEN correctly on the mutated run and stayed PASS unmutated; --json + --format junit together produced valid JSON and well-formed XML from one live run. Closed a real verification gap found during this audit: ORDER_VIOLATION had never actually fired in the new contract-based evaluator this session (only inferred by reading the code) -- fired it for real via a reversed-order-biased gate prompt on customer-refund, correctly FAILed at step 1 with 0 real tool executions. Full ranked list of what remains broken, unproven, or unfinished is in the final report delivered in chat, not repeated here.
2026-09-14 | CC | Proved --storage aws against the real deployed agent-replay-dev stack. Recorded vulnerable-dependency (5 runs, authorized) to AWS; confirmed via direct dynamodb get-item and s3 list-objects that items/objects land under the right table and golden/ prefix with the new Phase-5 metadata fields present and CI fields correctly NULL/omitted for a local invocation. agent-replay test/replay/five-times-test --storage aws all PASSed, exit 0. Found and fixed two real bugs on first contact, not papered over: (1) cmd_replay never actually checked spike/agent.py COUNTS during its run phase -- it could report PASS without verifying the zero-network claim at all; added the same load/run-phase boundary spike/replay.py already used, now verified 0 sockets/0 bedrock/0 tool-bodies for real against both backends. (2) agent-replay test with no scenario arg (exactly what CI runs) discarded EVERY already-computed result the moment any one scenario could not be evaluated -- first via an uncaught SystemExit from a missing AWS recording, then via an uncaught live-model exception (a real Bedrock modelStreamErrorException hit during testing) -- both silently rendering as a misleading overall PASS once results was empty and all([]) has vacuous truth. Fixed by containing both failure classes per-scenario (ReferenceLoadError, and a broad catch around run_and_evaluate), threading a new verdict=ERROR entry through report.py/render_pr_comment.py so a scenario that never ran renders distinctly (warning icon) and correctly forces the overall comment to FAIL, not PASS by omission. Deployed the CommitIndex GSI (infra/template.yaml only, as authorized); proved it for real: wrote a throwaway item with commit_sha set (real DynamoDB+S3, fabricated trace content, zero Bedrock cost) and queried CommitIndex directly, got back exactly that one item with the right projected attributes; cleaned up the probe item and its S3 objects afterward. Resolved the s3:HeadObject question definitively (it is not a real IAM action; HeadObject is authorized by s3:GetObject, which the policy already grants separately) and tightened infra/github-oidc.yaml to the exact action set spike/storage.py actually calls (dropped unused dynamodb:GetItem/PutItem, dropped the non-functional s3:HeadObject) -- still not deployed. Full recommendation on committing traces/ vs AWS-only vs a hybrid delivered to the user in chat, not implemented pending approval.
2026-09-14 | CC | Phase 1 fixes. (1.1) Goldens hybrid: .gitignore now excepts the 7 curated reference traces for vulnerable-dependency/customer-refund (git add -f staged, not committed), README documents the trade-off (contract is the reviewable artifact, traces churn on every re-record so only the two stable demo scenarios are committed); verified via a from-scratch fresh-clone simulation built from exactly `git ls-files --cached` + `--others --exclude-standard` (what a real commit right now would contain) -- fresh venv, fresh install, agent-replay test vulnerable-dependency/customer-refund both PASS with zero AWS and zero manual trace copying. (1.2) Transient Bedrock errors: added _is_transient()/_run_scenario() (cli.py) -- retries once on a transient-looking exception, only reports ERROR if the retry ALSO fails, with a transient flag that is False if the retry fails with a DIFFERENT (non-transient-looking) error. Threaded through everywhere a human reads it: the CLI (_print_error, a distinct ERROR banner, separate from PASS/FAIL), report.py (JSON transient:bool, JUnit <error type=TransientError> vs ReferenceLoadError), and the PR comment (a distinct icon/label and explanation, plus the top banner reads FAIL (transient only) when every non-pass is transient). Verified by injection: a fake exception matching a transient marker retries exactly once and, if it fails again with an unrelated error, is correctly NOT marked transient; if it fails again with the SAME transient signature, IS marked transient; a non-transient error never retries; a transient error that succeeds on retry surfaces no error at all. (1.3) Audited every claim the CLI/JSON/JUnit/PR comment makes: all but two are directly read from data computed moments earlier (GateToolTap/ReplayableBedrockModel/ToolTap counters, the just-written contract text, the just-completed Contract objects) -- no unchecked assertions found beyond the cmd_replay zero-network gap already fixed last session. Found and fixed one real omission (not a false claim, a silent drop): ContractStep never carried gate_mutated at all, so a --mutate run showed no indication of which step was altered, unlike the original gate.py which always did -- added the field, verified a real --mutate run now marks the mutated step MUTATED in the CLI table and true in JSON.
2026-09-14 | CC | Phase 2 dashboard reads real runs. Added agent_replay/dashboard.py (maps evaluate.py's ContractResult onto the web app's existing GateRun/ComparisonResult shape unchanged, per instruction -- no new fields invented) and wired cmd_test/cmd_gate to persist every --storage aws run via spike/storage.py's new save_test_run/save_comparison/record_acceptance. Wrote agent_replay/lambda_handler.py self-contained (boto3+stdlib only, deliberately NOT importing spike/storage.py to avoid pulling in the full strands-agents SDK) implementing GET /runs/{id}, GET /commits/{sha} (CommitIndex GSI query), GET /scenarios, POST /runs/{id}/accept, plus infra/dashboard-api.yaml (API Gateway HTTP API + Lambda + role). Deploy BLOCKED: CLI_Agent_Replay lacks apigateway:POST and iam:DeleteRolePolicy, leaving stack agent-replay-dashboard-api stuck ROLLBACK_FAILED -- reported verbatim, not self-remediated. Built agent_replay/dev_api_server.py (stdlib http.server routing into the exact same lambda_handler.handler()) as a verification stand-in against real AWS data while deployment is blocked. Also fixed a real GSI schema gap found by querying live: CommitIndex's projection never included verdict/accepted_by/accepted_at (predates those fields) -- required a two-step CloudFormation deploy (DynamoDB forbids updating a GSI's projection in place) which WAS carried out on the real dev stack, verified via a live query. Web: added api.ts/router.tsx, /commit/:sha (triage rail) and /run/:runId routes, RunView.tsx (reuses Engine/ForkOverlay/NodeCard/Timeline/Hero unchanged -- "data-source change, not a redesign"), loading/error states (StateScreen.tsx). PR comment's "View the fork" now links to /commit/{sha}. Verified live end-to-end against real --storage aws data (a real PASS and a real FAIL produced via simulated CI env vars over the existing authorized reference pool, no new recording): triage rail, fork, Inspector, and scrubbing on a real (larger) trace all confirmed working in-browser. Fixed one real layout bug found during that verification: RunView's root reuses the fixture demo's .stage class (position: fixed), which painted over the triage rail regardless of CommitPage's flex layout -- added a .stage-embedded override (position: absolute within .triage-main's position: relative) rather than forking the component.
2026-09-14 | CC | Phase 3 accept in the dashboard. Added web/src/engine/contractDiff.ts (buildContractDiff maps MISSING_STEP/UNRECORDED/UNSOURCED_ARGUMENT to a single acceptable requires/permits line each; ORDER_VIOLATION/FORBIDDEN are shown for context but marked non-acceptable -- neither reduces to one line) and DiffPanel.tsx (per-line Accept toggle, no accept-all anywhere, only visible on FAIL). Chose a downloadable printed patch over a full contract file: the dashboard only ever fetches the comparison RESULT, never the contract's full YAML text, so it cannot honestly offer a drop-in replacement -- buildPatch() renders +/- lines in callsig.py's own tool(arg=value) format, small enough to review in seconds and apply by hand, matching this project's own stated philosophy for the contract file itself. Accepting calls POST /runs/{id}/accept (spike/storage.py's record_acceptance), writing accepted_by/accepted_at into the run's DynamoDB item -- confirmed by direct get-item. Verified end-to-end on the real FAIL run from Phase 2 (fade111, MISSING_STEP on strands-agents-tools at two steps): accepted both lines in the UI (one Accept click each, confirming the no-bulk-accept design holds under a real multi-change diff), confirmed the accept API call and DynamoDB write, then manually applied the equivalent patch to agent-replay/contracts/vulnerable-dependency.yaml and re-ran `agent-replay gate` with a prompt reproducing the same regression (skip strands-agents-tools) -- PASSed, confirming the patch is correct and sufficient. Reverted the contract file afterward: this was a verification of the patch's correctness, not a real acceptance decision, which stays a human's call per the "no dashboard writes to git directly" design.
2026-09-14 | CC | Phase 1 (real deploy): IAM fixed by the user (AmazonAPIGatewayAdministrator, IAMFullAccess; stuck agent-replay-dashboard-api stack deleted). Confirmed the old stack gone, rebuilt dashboard-api.zip, deployed infra/dashboard-api.yaml for real -- succeeded first try. Real API: https://337k8w3bbi.execute-api.ap-south-1.amazonaws.com. All four endpoints curled against real data and confirmed correct, including a real POST /accept verified by a direct dynamodb get-item (then reverted to the prior genuine value). Pointed the web app at it via web/.env.local (VITE_AGENT_REPLAY_API_URL), fixed a real gap found along the way -- .gitignore only covered bare .env, not .env.local -- before it could leak. Screenshotted the triage view loading from the real deployed API with the URL visible. Found and fixed the one real behavioral difference from the local stand-in: GET /runs/{unknown_id} returned 500 AccessDenied on the real Lambda (its role lacked s3:ListBucket, so S3 won't disclose object non-existence, per S3's own documented behavior) where the local stand-in (running under broad dev credentials) returned a clean 404 -- added a scoped s3:ListBucket (bucket-level resource, s3:prefix condition limited to comparisons/*) rather than papering over it by catching AccessDenied as not-found, per instruction (masking a permissions failure as missing data is the worse failure mode). Separately fixed a real leak: the Lambda's top-level exception handler echoed raw boto3/IAM error text (role ARNs, policy detail) straight to the client in every uncaught-exception path -- now logs full detail to CloudWatch via logging.exception and returns a generic message. Hit a real CloudFormation gotcha while redeploying: it only diffs a Lambda's Code.S3Bucket/S3Key, so reusing the same S3 key meant the IAM policy update landed but the code did not -- forced with aws lambda update-function-code, confirmed via CodeSha256 match. Audited every AWS call the Lambda makes against the deployed role: all five now confirmed working live (GetObject+ListBucket, Query on CommitIndex, Scan, UpdateItem); could not independently confirm CloudWatch log delivery -- my own CLI user lacks logs:DescribeLogStreams, unrelated to the Lambda's own role.
2026-09-14 | CC | Phase 2 PR-readiness audit (no commits/pushes made, per instruction). Grepped everything that would be pushed for AWS account IDs, ARNs with account numbers, absolute paths, and credentials: clean, except one pre-existing, already-committed absolute-path leak in web/src/fixtures/*.ts (hardcoded D:/Project/agent_replay in demo fixture data, from commit 20a2653, predates this task) -- reported, not fixed (fixture data, not live config, low severity, but a real local-path disclosure). Confirmed the 7 curated trace files stage correctly per the hybrid decision. Walked infra/github-oidc.yaml against spike/storage.py's actual boto3 calls the way Phase 1 did for the dashboard-api role, and found the same class of gap: the comment's "1:1 mapping" was accurate when written but predates Phase 2's save_test_run(), which calls Table.put_item() directly -- dynamodb:PutItem was missing, meaning EVERY --storage aws test/gate call in CI (not just recording) would fail persisting dashboard data. Fixed in the template (not deployed -- that stack is the user's to deploy by hand per Phase 2 rules). Found one CI-blocking data gap, not a code bug: only vulnerable-dependency has any --storage aws reference recording in the real account (confirmed via direct DynamoDB lookups, all 5 runs present); customer-refund and vulnerable-dependency-mixed have zero -- `agent-replay test` (no scenario arg, exactly what CI runs) would ERROR on both from a missing reference recording, not from any real regression, making a clean green baseline across all three scenarios currently impossible. Verified path-portability is NOT a problem on Linux (agent_replay/portable.py's {repo} token expands via Path.as_uri()/str(), OS-correct by construction) and pyproject.toml's dependencies are self-contained for a fresh `pip install -e .`. Full walkthrough, remaining decision points, and exact hand-off commands delivered to the user in chat.
2026-09-15 | CC | Pre-Phase-3 verification against the real deployed GitHub repo (Paritoshcodes/test-agent-replay) and dashboard (Amplify, master.dd7tq9rrv97fi.amplifyapp.com). Scoped the CI workflow to `agent-replay test vulnerable-dependency` (was every scenario) per the Phase 2 finding that only this scenario has AWS reference data -- otherwise every real CI run would ERROR on the other two from missing data, not a regression. Verified live against the real deployed app, not assumed: navigated to both /commit/{sha} and /run/{id} on the real Amplify URL and confirmed both load real data from the real API Gateway (confirmed the exact URL baked into the built JS bundle) -- the SPA rewrite works on both routes, not just one. Found one real drift while re-walking infra/github-oidc.yaml with concrete values: the file as actually pushed to origin/master still has MaxSessionDuration: 900, below AWS's own IAM minimum (3600) -- the deployed stack is correctly running 3600, meaning someone corrected it at deploy time without the fix reaching the repo; my local copy now has the correct value, not yet committed (that is the user's call, per instruction). Confirmed the OIDC role's deployed GitHubOrg/GitHubRepo parameters exactly match the real repo. Prepared the green-baseline branch's actual content: the workflow scoping fix plus a real (not manufactured) README.md correction -- the AWS-and-CI section still described testing every scenario, now stale against the scoping fix -- and the MaxSessionDuration drift fix. All three are CI/docs-only; nothing in the comparator, the agent, or any contract changed.
2026-09-15 | CC | Phase 0 viability (no agent_replay changes, per instruction). Cloned aws-samples/sample-multi-agent-customer-service-for-bedrock as a sibling directory, own venv, pip install -e . (strands-agents 1.55.1 / strands-agents-tools 0.8.8, same versions this repo already uses). Configured via env vars only, no code touched: AWS_REGION=ap-south-1, SUPERVISOR_MODEL_ID/SPECIALIST_MODEL_ID=apac.amazon.nova-lite-v1:0, no GUARDRAIL_ID/STRANDS_KNOWLEDGE_BASE_ID. Confirmed by reading the code (agents/config.py's guardrail_enabled/knowledge_base_enabled) AND by running it live: unset guardrail/KB means "runs unguarded/ungrounded with a logged warning", never a crash. Verified the README's APAC/Claude-4.6 claim directly via `aws bedrock list-inference-profiles` rather than trusting it: no apac.anthropic.*-4-6 profile exists (confirmed true), though a global.anthropic.claude-*-4-6 profile does exist in this account -- irrelevant to this task since config.py's own geo-detection has no path to "global" anyway, noted for honesty. Confirmed via agents/specialists.py that scheduling's tool list has no retrieve at all, and billing's retrieve is only registered when a KB id is configured -- structurally, not just empirically, incapable of calling it here. Ran the real scheduling query 5x and a billing query 2x through the real, unmodified supervisor (a callback_handler-based probe script, kept outside the sample repo, mirroring demo/app.py's own TraceCollector pattern) against live Nova Lite: zero crashes across all 7 live runs; zero retrieve calls anywhere; the one consequential action each scenario takes (check_availability / check_balance) had 100%-identical arguments across every run. The only variance was the optional classify_intent pre-step (called 3/5 scheduling runs, skipped 2/5 -- genuine model discretion per its own prompt wording, not a bug) and one run (5th) where the supervisor redundantly called scheduling_specialist twice with slightly reworded queries before proceeding. Full tool sequences and the viability verdict delivered to the user in chat; Phase 1 not started, pending approval.
2026-09-15 | CC | Phase 1 package refactor. Moved all 9 spike/*.py files into agent_replay/_spike/ (git mv, history preserved) and converted every bare `from agent import ...`/`from gate import ...`/`from storage import ...`/`from gate_compare import ...` -- both within the moved files and in agent_replay/*.py -- to proper package imports; removed paths.ensure_spike_importable()/SPIKE_DIR/the sys.path insertion entirely. Import-mechanics only, verified by re-reading every changed line for anything that looked like a logic change; none found. Reading the code (not just moving it) surfaced two real, latent path-resolution bugs the move would have silently introduced: storage.py's TRACES_DIR and audit_agent.py's DEFAULT_TARGET were both `Path(__file__).resolve().parent.parent` -- correct only when spike/ was a sibling of agent_replay/ (the editable-install assumption); after the move that resolves to agent_replay/'s own directory, not the repo root. Fixed both to be cwd-based (Path.cwd()), matching paths.data_dir()'s own existing convention; also fixed the same class of bug in agent_replay/portable.py's {repo} token itself, which is meant to mean "the project you're running agent-replay from" per its own README documentation, not "wherever the package is installed" -- renamed REPO_ROOT to a project_root() function (Path.cwd()) since a git-URL (non-editable) install makes the old assumption actively false, not just theoretically weak. Verified pip install -e . still works in this repo (agent_replay._spike.* submodule correctly auto-discovered by the existing "agent_replay*" setuptools find pattern, no pyproject.toml change needed) and re-ran the full suite: replay (0 network, byte-identical), test x5 (5/5 PASS live Bedrock), customer-refund's forbids rule (both PASS unmutated and FORBIDDEN when forced), --json + --format junit combined (valid JSON, well-formed XML). Verified the git-URL install claim FOR REAL, not reasoned about: confirmed the CURRENT pushed (pre-refactor) code fails exactly as expected when installed via the real https://github.com/Paritoshcodes/test-agent-replay URL into a clean venv (RuntimeError: spike/ not found, must be installed editable) -- a clean "before" baseline; then, since pushing this session's uncommitted refactor was not authorized, committed it to a disposable LOCAL git repo (never touching this repo's real history) and installed via `pip install git+file:///...` into a separate clean venv -- the exact same pip/git install code path as git+https, pointed at a local remote instead -- confirming agent-replay --help works non-editable. Went further: copied only agent-replay/scenarios.yaml+contracts+traces+requirements.txt (no agent_replay source) into that clean venv's own directory (nothing to do with the package's install location) and ran BOTH the zero-network replay proof and a real live Bedrock test from there -- both passed, proving {repo}/TRACES_DIR resolve correctly under a genuinely different cwd, not just "should". Created git tag ci-loop-working at the pre-Phase-1 HEAD as the STOP CONDITION's revert point (did not exist before this session; instruction referenced it as if it did) -- local only, not pushed.
2026-09-15 | CC | Phase 2 adapter. Verified against the installed strands-agents 1.55.1 SOURCE (not docs) before writing any code: HookRegistry.add_hook(provider) is public and works identically whether called at Agent construction or after (Agent.__init__ does the exact same thing internally); agent.model is a plain attribute read FRESH at call time (event_loop.py passes `model=agent.model` into the live invocation context on every turn) so a post-construction swap takes effect; HookRegistry has NO public remove/unregister method at all (checked registry.py in full) -- ScopedInstrumentation.detach() therefore disables OUR OWN hook via an `active` flag (added to _spike.agent.ToolTap and _spike.gate.GateToolTap, defaulting True so every existing caller is byte-identical) rather than reaching into the SDK's private _registered_callbacks, and restores the agent's ORIGINAL model object, which is a complete real reversal. model_id lives at agent.model.config["model_id"] (BedrockModel's own internal code reads it from exactly there); region does NOT live in .config (a separate constructor arg) so it's read off the live boto3 client instead (model.client.meta.region_name). Built agent_replay/adapter.py (build_from_ref for module:callable resolution, ScopedInstrumentation, model_id_of) and agent_replay/paths.py's import_agent_module (bare-name form: tries a normal top-level import first, falls back to agent_replay._spike.<name> -- disambiguated from the new form by a single ":" check, since a bare module name can never contain one) -- wired into both cli.py's cmd_record and evaluate.py's run_and_evaluate via an is_ref() branch, old bare-module path left untouched in its own branch. Added the `agent` field to every captured event (2.3) without touching _spike/*.py's event shape at all: stamped post-hoc (`e.setdefault("agent", ...)`) once a run's full trace exists, "supervisor" for the adapter path (accurate for this phase -- see LIMITATIONS.md's opaque-specialists entry) and the scenario's own module name for the bare-module path. Added callsig.py's call_key(tool, args, *, strategy="exact") seam (2.4) -- registered strategies dict, "exact" is the only one and is byte-identical to the old unconditional canonical(args) call. Found and fixed a real bug during verification, not before: agents.supervisor.get_supervisor() (scenarios.yaml's OWN documented example factory) is a process-wide singleton -- confirmed empirically (calling it twice in one process returns the same object; Agent.messages, a plain list, grew 6 then 12 across two calls on that same object) -- meaning N "independent" recorded runs would have silently become one growing N-turn conversation. Fixed by resetting agent.messages = [] in ScopedInstrumentation.attach() (harmless no-op on a freshly-built non-singleton agent). VERIFIED PHASE 2 for real: installed from a disposable local git+file:// repo (same reasoning as Phase 1) into a clean venv, the sample repo's own `agents` package pip installed alongside it, a genuine adopter-shaped project directory (sibling to the sample repo, its own agent-replay/scenarios.yaml, nothing else). Recorded 5 real runs each of billing-balance and schedule-meter-reading; billing-balance derived cleanly (requires: classify_intent + billing_specialist, permits empty, both stable at 5/5) and tested 5/5 PASS. schedule-meter-reading reproduced the EXACT failure mode the task predicted rather than the hoped-for one: this specific 5-run sample also landed classify_intent in requires (5/5 again, not 3/5 like Phase 0's exploratory sample) -- reported as unlucky, not silently accepted -- and a subsequent live test run 4/5 genuinely FAILED with MISSING_STEP on classify_intent when the model's own real discretion skipped it, a false-positive regression signal caused entirely by finite-N derivation, not a real behavioral change. Full numbers, both contracts verbatim, and the free-text keying investigation (2.4, using already-collected Phase 0 data, no new calls) delivered to the user in chat and in two new docs/LIMITATIONS.md sections.
2026-09-15 | CC | Phase 3 nested agents -- RECORD mode works, verified; TEST/GATE mode does NOT, and I stopped rather than ship it, per instruction. Investigated two mechanisms against the installed SDK before writing anything: patching strands.Agent.__init__ (the OTel auto-instrumentation pattern) does not apply here -- verified empirically that get_supervisor() returns the SAME supervisor AND the same specialist objects on every call in one process (agents/supervisor.py's own singleton), so no Agent() construction happens to intercept after the first run; an __init__ patch active only around agent(prompt) would silently instrument nothing on every subsequent run. Closure introspection DOES work, verified for real: strands.tools.decorator.DecoratedFunctionTool (what @tool produces) stores the original function on `._tool_func`; a plain function's closure (`__closure__`/`__code__.co_freevars`) is ordinary public Python introspection, not an SDK internal, and a live check against the real sample repo confirmed `billing_specialist`'s closure contains exactly one cell whose contents IS a real Agent instance. Built agent_replay/adapter.py's find_nested_agents() (scans agent.tool_registry.registry, keyed by TOOL NAME) and extended ScopedInstrumentation to recurse one level (matching the sample repo's own topology: supervisor -> specialists, specialists do not nest further), attaching/detaching a real nested instrumentation per discovered agent, including the SAME messages=[] singleton-conversation fix (verified the specialist objects are ALSO reused across calls, same bug, same fix). Added agent_name (stamps agent_replay/_spike/agent.py's ToolTap and _spike/gate.py's GateToolTap events immediately at capture time when set, None default is byte-identical to before) and a SHARED step_counter for GateToolTap (a one-element list, not a plain int -- multiple tap instances writing into one shared trace would otherwise hand out duplicate gate_step numbers, a real bug caught before it shipped, not after). Made contract rendering agent-qualified (callsig.render_call/parse_call, contract.py's Contract.agent_by_key) as PURE DISPLAY ONLY -- the underlying (tool, args) matching key is completely unchanged, deliberately, to keep every existing single-agent contract byte-for-byte identical (verified: vulnerable-dependency still loads, replays 0-network, and tests PASS live, unchanged). VERIFIED PHASE 3.3 for real: re-recorded billing-balance with nested capture (git+file:// disposable install again, same method as Phases 1-2) -- billing_specialist.check_balance(account_number=ACC-100123) appears in requires exactly as required, contract now correctly qualified (supervisor.*/billing_specialist.*). STOPPED at 3.4: `agent-replay test`/`gate` on this SAME contract FAILs 2/2, unconditionally, not flakily -- MISSING_STEP on check_balance every single time. Root cause verified, not guessed: GateToolTap's _before ALWAYS substitutes either a stored golden result OR a synthetic error for every intercepted call, with no third option to let the real tool run -- so the instant the supervisor's OWN billing_specialist call is intercepted (matched or not), the specialist's real function body (which is what would call check_balance) never executes, unconditionally, regardless of match outcome. This is not a bug introduced by this phase's code; it is gate/test's own foundational design (freeze every tool's output so only one variable -- the thing under test -- ever changes) colliding with a "tool" that is itself a live sub-agent. Retracted an earlier premature claim: escalation-complaint's forbids rule (transfer_to_agent when create_ticket.category == BILLING_DISPUTE) showed PASS on a live test, but re-examination showed this was a vacuous pass -- the same bypass meant create_ticket/transfer_to_agent were never attempted that run, so the rule evaluated over zero forbidden-tool calls, proving nothing. Did not attempt a redesign of GateToolTap's injection semantics (e.g. a "pass-through" tool category) without authorization, per the explicit stop condition. Phase 4.1 measured for real (independent of the above, RECORD mode only): schedule-meter-reading recorded fresh at --runs 15, classify_intent called in 13 of 15 (confirmed two ways -- the derived contract's own permits annotation and a direct count across all 15 saved trace files, in agreement). At a true ~87% per-call rate, P(5/5 all hit) is approximately 0.49 -- a coin flip -- explaining why the earlier 5-run sample looked stable when it was not; P(10/10) is still approximately 0.24. Recommendation (4.2, not implemented, pending approval): raise the bar for `requires` to "seen in every run AND N meets a minimum (suggest 10)", not just "record more by default" -- the latter reduces but never eliminates false promotion at any realistic live-model-cost N, per the math above. Trade-off stated plainly: a higher requires bar means fewer false-positive gate failures but also means a genuine regression on a call with real natural variance sits in permits and is NOT flagged if it stops happening -- protects gate trustworthiness at the cost of catching regressions specifically on already-flaky calls. Not implemented; 4.3's 10-run re-verification withheld pending the user's choice, per instruction.
2026-09-15 | CC | Decision 1, pass-through tools. scenarios.yaml gains `pass_through: [tool names]` (agent_replay/scenarios.py) -- a listed tool EXECUTES FOR REAL during test/gate (agent_replay/_spike/gate.py's GateToolTap._before returns immediately without touching event.selected_tool, a new third branch alongside inject/synthesize) while its OWN nested tool calls are still injected from golden exactly as any other call, via a nested GateToolTap attached through adapter.py's ScopedInstrumentation (now pass_through-gated: an unlisted discovered nested agent is left frozen, not recursed into). Auto-detection warns, never auto-adds: adapter.warn_uninstrumented_nested fires at both record and test time when find_nested_agents sees a sub-agent tool not in pass_through -- silence is exactly how the earlier vacuous forbids pass happened. Chose WEAKLY_ATTRIBUTABLE as the label for anything at/after a pass-through call (evaluate.py's _evaluate_steps, a new pass_through_boundary alongside the existing attribution_boundary, computed as min() not next()/first-seen because a pass-through call's own step is assigned before its nested calls begin but the shared trace's list order can interleave the nested call's fully-appended event first): distinct from UNATTRIBUTED (the harness's own synthetic error has contaminated everything downstream, a real divergence) because nothing has diverged here -- the sub-agent's OWN live model is simply also a variable now, on top of the change under test, even though every leaf tool result underneath it was still frozen. Threaded through the human report (a new "pass-through boundary" line), JSON (ContractResult.pass_through_boundary), and the PR comment (flows through automatically, attribution is already a plain string field there). VERIFIED PHASE 1.4 for real (disposable git+file:// install again): agent-replay test billing-balance now PASSes with check_balance visible and genuinely injected (was: unconditional FAIL, every time, before this decision). The specialist forbids rule fired NON-VACUOUSLY -- but only after discovering transfer_to_agent's own ticket_id is a freshly-minted unique value every call, structurally incapable of ever exact-matching regardless of reference pool size, so the demo target moved to create_ticket (whose own args have no such field) conditioned on classify_intent's real, deterministic output ("create_ticket when classify_intent.intent == ESCALATION") -- proved firing for real: step 3 FORBIDDEN create_ticket, create_ticket genuinely captured as a real event in the printed step table, not inferred. Also found and worked around, not silently: forbids evaluation only ever runs on a call ALREADY in requires/permits (evaluate.py's if/elif chain checks requires/permits membership before forbids), so free-text arguments (already documented, Phase 2.4) can ALSO defeat forbids checking specifically, not just requires/permits promotion -- create_ticket needed a widened reference pool (15 runs) before its own free-text issue_summary matched often enough to be checkable at all. The nested --mutate demo: mutated check_balance's balance_gbp to 9999.99; the supervisor's final answer changed from "...is GBP142.50..." to "...is GBP9999.99...", propagated correctly through two live reasoning layers (specialist then supervisor), with MUTATED correctly marked on check_balance's own step -- a stronger demonstration than a top-level mutation, as expected.
2026-09-15 | CC | Decision 2, confidence-based derivation bar (contract.py). Exact rule: a key unanimous across N runs promotes to requires immediately if this SAME recording shows no variance anywhere else (an otherwise-empty permits set -- no evidence this agent/scenario has any real discretion, vulnerable-dependency's own actual historical shape at N=3 and N=5 alike, confirmed unaffected by re-deriving it locally, byte-identical output); the moment any other key in the same batch DOES vary, every other "unanimous so far" key is held to a confidence bar instead -- N must be at least 17 (math.ceil(log(0.10)/log(0.87)), derived from this project's own worst measured real-discretion rate and a chosen 10% accepted false-promotion risk, not picked from nowhere) before unanimity is trusted. A held-back key stays in permits with an explicit "confidence bar not met" annotation, and the contract header explains the policy in plain language whenever it actually held something back (unaffected contracts render byte-for-byte unchanged, nothing new to explain). Trade-off recorded near-verbatim in docs/LIMITATIONS.md: a genuine regression on a call with real natural variance now sits in permits and goes unflagged if it stops happening -- a real, stated cost, not buried. Had to re-record schedule-meter-reading from scratch rather than re-derive from the existing 15 runs as instructed -- those trace files were destroyed by this session's own end-of-phase cleanup before this message arrived; reported plainly rather than silently substituted. The fresh real measurement differs from the earlier one (classify_intent 7 of 15 this time, not 13 of 15) -- itself a real, honest data point about how much this rate can swing batch to batch, not smoothed over. classify_intent correctly lands in permits either way; check_availability and scheduling_specialist, both 15/15 unanimous, are correctly held back too, since 15 < 17. Re-ran agent-replay test TEN times (not five) against the re-derived contract: 10/10 PASS, exit 0 every time -- the flaky gate (Finding 2, prior session) is fixed.
2026-09-15 | CC | Phase 0, two fixes plus the data-destruction root cause. (1) Confidence bar made PER TOOL, not per recording (contract.py): a key unanimous across N runs promotes to `requires` outright if its OWN tool shows no variance anywhere else in this recording; only a tool that DOES vary elsewhere holds its unanimous keys to a confidence bar sized to that TOOL's own observed rate in this recording (not a rate borrowed from a different tool, as the prior global `_WORST_OBSERVED_DISCRETIONARY_RATE=0.87` constant did). Every requires/permits line now carries its own one-line reasoning comment. Verified against vulnerable-dependency-mixed's own 5 existing traces: the OLD global rule's on-disk contract has 4 requires; the new per-tool rule correctly drops the pinned-package entries to permits (their tool, get_package_info, varies elsewhere on the unpinned packages) -- 2 requires/12 permits. vulnerable-dependency (zero variance anywhere) unaffected: requires=7/permits=0/order=3 either way, live test PASS. (2) Found, mid-task, that BOTH schedule-meter-reading's 15-run recording AND billing-balance's recording had been destroyed a second time by the same end-of-phase `rm -rf` cleanup pattern as the prior phase -- reported immediately rather than substituting silently, per instruction. Root cause identified as structural, not a one-off slip: cwd-based path resolution (agent_replay/paths.py's old project_root(), agent_replay/_spike/storage.py's old TRACES_DIR) meant a scenario's traces could live in whatever directory happened to be cwd when it was recorded, with nothing distinguishing "disposable install scratch" from "irreplaceable reference recording" before cleanup. Fixed properly, not papered over: project_root() now does an upward marker search (agent-replay/scenarios.yaml, then pyproject.toml, then .git -- first match wins, matching pytest's rootdir/npm's package.json approach), overridable via --project-root or AGENT_REPLAY_ROOT for monorepos/CI; traces moved from a top-level traces/ sibling to agent-replay/traces/, next to the contracts they back (migrated the 7 committed goldens via git mv, .gitignore's hybrid exceptions updated to the new path); added `agent-replay where` plus a "project root: ..." startup line on record/test/gate so the resolved directory is never a guess. Verified for real, not reasoned about: `agent-replay where`/`test` give the identical answer run from the repo root, from a subdirectory, and from an unrelated cwd via --project-root; a from-scratch fresh-clone simulation (exact `git ls-files`-plus-untracked snapshot, fresh venv, fresh install) reproduces vulnerable-dependency/customer-refund PASS unchanged. Confirmed the adapter case specifically: agent-replay installed editable and pointed at the sample-multi-agent-customer-service-for-bedrock repo (a sibling checkout, untracked additions only, nothing committed/pushed there) resolves its OWN project root to the SAMPLE repo, so agent-replay/scenarios.yaml, contracts, and traces for billing-balance/schedule-meter-reading now live permanently inside that repo's own tree -- not a disposable scratch directory -- fixing the recurring cause, not just this instance. Re-recorded both scenarios there (5 and 15 real runs): billing-balance unchanged in shape (requires=3: classify_intent/billing_specialist/check_balance, all trusted outright, live test PASS with check_balance visible exactly as Decision 1 proved). schedule-meter-reading's fresh 15 runs show a REAL, different distribution than either prior measurement (classify_intent 13/15 in Phase 4.1, 7/15 after the first re-recording, now 13/15 again by coincidence) AND a new finding: check_availability itself was only called in 14 of 15 runs this time, not unanimous at all -- live model variance, not a derivation-bar artifact, reported as measured rather than tuned to match the expected requires=2/permits=1 shape. Result: requires=1 (scheduling_specialist, unanimous, its own tool shows no variance), permits=2 (check_availability 14/15, classify_intent 13/15, both simply non-unanimous, the per-tool confidence-bar branch not exercised by this particular recording). `agent-replay test schedule-meter-reading` run 10 times against it: 10/10 PASS, exit 0 every time.
2026-09-15 | PS | Phase 0 approved with three notes, none requiring code changes beyond docs. (1) schedule-meter-reading's check_availability at 14/15 is a real finding about the sample agent, not something to engineer around -- added a docs/LIMITATIONS.md section: a contract can only be as strict as the agent is consistent, and this scheduling agent skipping a calendar check on an identical query once is a real bug in that agent's domain, correctly left in permits, not smoothed over. (2) Hero scenario confirmed as billing-balance (three trusted-outright requires including check_balance, stable); schedule-meter-reading kept deliberately as the honest counterexample -- what the tool reports when an agent is too variable to gate strictly. (3) agent-replay/contracts/vulnerable-dependency-mixed.yaml is confirmed known-stale under the Phase 0 per-tool confidence rule (its on-disk 4 requires/10 permits predates that rule; re-derivation locally showed 2 requires/12 permits) -- NOT re-derived on disk, since it is outside the demo path and re-deriving now would only churn a committed file for no benefit. Noted here rather than fixed.
2026-09-15 | CC | Phase 1, timeline redesign. Backend: added `agent`/`membership` fields to ContractStep (evaluate.py, threaded through report.py/dashboard.py/contract.py's load() so a MISSING_STEP synthetic step can still recover its agent from the contract's own agent-qualified lines) -- the only backend change Phase 1 needed, since nesting itself is reconstructed entirely on the frontend from these two fields plus the existing gate_status/tool/step (a pass-through step's own tool name equals its children's agent name, at any nesting depth, not just the one level the real adapter produces). Frontend: built a SEPARATE swimlane engine (engine/swimlane.ts's model + reconstruction, engine/SwimlaneEngine.ts's physics/layout/zoom/pan/collapse, engine/swimlaneRender.ts's canvas painter) rather than generalizing the old two-lane Engine.ts in place -- its physics (the tear/heal spring, the shockwave field) are entangled with exactly-two-lanes in a way that doesn't generalize, and the fixture demo at "/" was never the thing with the layout bug, so it and the old engine are untouched. New: per-agent swimlanes (DFS-ordered, nested lanes directly under their parent, tethered by a vertical connector to the exact node that spawned them), collapsible lanes (a self-collapsed lane keeps its own header+count badge, a lane folded by a collapsed ANCESTOR disappears entirely, zero height) with animated collapse springs, a real zoom/pan viewport (discrete 1/2/4/8/16x levels plus fit-to-width, wheel-to-zoom-at-cursor, shift+wheel or background-drag to pan, playhead auto-follow that re-centers the viewport when the playhead nears its edge) and a required minimap (own small canvas, full-domain density dots, draggable/resizable viewport window) -- together these are what actually fix the reported bug (a 40-step run has nowhere to overflow to anymore; only a bounded window renders at readable spacing at any zoom level). Node markers encode requires-vs-permits directly (filled dot vs hollow ring), not a tooltip, per this message's explicit addition. Both side panels (Inspector; CommitPage's triage rail) are collapsible with a persistent reopen tab, driven by the same --side-w CSS var Hero/RunHeader already read (0 when collapsed) so the lane region's own width calc needs no special-casing; the Inspector switches to an overlay drawer under 980px instead of reserving width. Built a synthetic 40-step/3-level-nested fixture (fixtures/synthetic.ts, plus 1-step and very-long-names/args fixtures) and a dev-only /dev/:name route to render them through the real RunView with no API call, since real recordings (7-8 steps, one level of nesting) cannot exercise any of this. Found and fixed FOUR real bugs during verification, not assumed: (1) Hero's title genuinely was crushing to "Ready t..." -- five fixed-min-width metric columns had `flex:none` against hero-left's `min-width:0`; fixed via a container query on .hero's own rendered width (reflects --side-w correctly) that hides metrics least-important-first instead of ever letting text truncate, plus raising hero-left's floor to 300px (measured against the longest real title string, not guessed). (2) node/lane labels only recomputed their zoom-dependent level-of-detail on a discrete UI-state change (step/playing/etc.), so a zoom-button click alone left them stuck at stale visibility until an unrelated re-render happened -- moved the decision into the same imperative per-frame anchor-writing pass the engine already uses for position/opacity (data-labelmode/data-current attributes), with CSS doing the actual show/hide. (3) a long free-text argument in a node label had no wrap/cap at all and could visually reach toward the Inspector's screen region -- fixed by wrapping labels within a bounded column (CSS, not truncation) and by giving the on-canvas label its own never-truncated argument summary function (fullArgSummary) distinct from the compact, legitimately-truncated one used only in the fleeting timecode description. (4) the narrow-viewport Inspector drawer's width used `88vw` (viewport width) instead of a value relative to its own possibly-embedded container (.stage-embedded, inside CommitPage's triage layout, can be narrower than the viewport) -- oversized and combined with its closed-state translateX(100%), this turned `.stage` into an accidental horizontal scroll container that a button's own focus/click could scroll sideways, visibly shifting and clipping unrelated content; fixed by sizing the drawer relative to `.stage`'s own width and switching `.stage`/`.triage-main` from `overflow:hidden` to `overflow:clip` (hidden still permits programmatic/focus-driven scrollLeft changes; clip does not). Also found and fixed, not just noticed: lane-collapse toggling mutated the Lane object directly with no React re-render path at all (aria-expanded/the chevron/the count badge showed stale state indefinitely) -- toggleLane() now also produces a new `ui` object reference and calls notify() so useSyncExternalStore picks it up, same mechanism the playhead already used. Verified for real: agent-replay test on vulnerable-dependency/customer-refund unaffected by the ContractStep field additions (still PASS, exit 0); a real `agent-replay test billing-balance`/`schedule-meter-reading --json` run confirmed the actual `agent`/`membership`/`pass_through_boundary` values now on the wire, not just on synthetic fixtures; all 6 requested edge cases screenshotted at both 1920x1080 and 1366x768 (40-step/3-level, 1-step, very-long-names, divergence near the right edge, and all four combinations of the two collapsible panels via a mocked /commit/:sha), confirming nothing renders underneath anything else in any of them; `tsc --noEmit` and `vite build` both clean.
2026-09-15 | PS | Phase 1 fixes after real-browser feedback: two functional bugs (lane collapse's real click target, label collision at fit zoom) and three visual problems (dead vertical space, per-node permit badge noise, undersized affordances). BUG 1: the collapse handler lived on an 18x18 chevron button, not the row -- SwimlaneStage's LaneHead is now one full-width <button> (chevron+name+summary all inside it), giving a real click target plus free native keyboard activation; the earlier "verified via aria-expanded" claim was hollow because it was checked with a synthetic btn.click() call, which bypasses real hit-testing entirely -- exactly the kind of vacuous verification this project's own philosophy warns against. BUG 2: rewrote the label policy (engine/labelPolicy.ts, pulled into its own pure DOM-free module, shared by SwimlaneEngine.ts and a new headless script) -- fit/low zoom shows nothing but the playhead's current node, the divergence node, and the hovered node; every other node's reveal threshold is now scaled by its OWN lane's node-count rank ("densest lanes last"), not one global density number. Measured, not eyeballed: scripts/verify-labels.ts (npm run verify:labels) builds the REAL swimlane model from the REAL synthetic-40 fixture, runs the REAL labelPolicyFor(), estimates each visible label's rendered width from global.css's own font-size/max-width values, and checks every same-lane same-slot pair for horizontal overlap across 2 widths x 5 zoom levels x 3 pan positions (83 placements) -- zero overlaps; a separate count confirms fit zoom shows exactly the 2 non-hover exceptions, nothing ambient. VISUAL 1: lane row height is no longer a constant -- engine/rowHeight.ts (also pulled out pure/shared, also independently verified headless via scripts/verify-layout.ts/npm run verify:layout) stretches open lanes to fill real available height (56-156px, few lanes real breathing room) and falls back to a 56px floor plus vertical scroll once more lanes don't fit; verified with real numbers: 4 lanes at 1920x1080 stretch to 156px each with zero leftover; 1 lane also stretches to 156px instead of sitting tiny in dead space; 12 lanes floor out and correctly overflow (112px at 1080p, 424px at 768p), scrolling instead of crushing further. Vertical pan reuses the SAME background-drag gesture already used for horizontal pan (extended to 2D) rather than adding a new modifier-key convention. VISUAL 2: removed the badge from every permitted node; added each lane's own "N required · M permitted" summary line to its header (schedule-meter-reading's real shape -- 1 required, 11 permitted -- is now legible as one line even at small scale) and restricted the per-node pill to the hovered/current node or the highest zoom tier only. VISUAL 3: widened the gutter (176px fixed -> 140/200/240px by viewport) and switched the lane name from ellipsis to wrap so "sub_verifier_agent" no longer truncates; the collapsed Inspector/triage-rail tabs got a chevron icon, solid fill, and a real drop shadow instead of reading as a thin, low-contrast sliver; the minimap's own top margin was tightened (the bulk of the perceived "gap" was VISUAL 1's dead space above it, not the minimap's own position). Also added, mid-fix: a canvas clip so a lane scrolled to the top of the stack disappears behind the ruler instead of drawing over its ticks (a real gap VISUAL 1's new vertical scroll would otherwise have introduced) -- the equivalent DOM-side edge case (a scrolled node label peeking into the ruler band) is NOT specially clipped, left as a disclosed minor cosmetic limitation, not fixed. Did not drive a live browser session for this round of fixes, per explicit instruction to control token spend -- instead built two permanent, real-code (not re-implemented) headless verification scripts and handed the user an exact manual verification checklist for anything only a live click/screenshot can confirm (the click itself, the visual polish, the 50%-scale legibility check).
2026-09-15 | CC | Phase 1, real-browser re-verification of everything in decision 43. BUG 1's full-row-button fix was necessary but not sufficient: SwimlaneStage's `.sw-stage` background-pan handler (bgDown) unconditionally called setPointerCapture on itself for ANY pointerdown that bubbled up to it, including one that started on the lane-header button -- hijacking that pointerId's eventual pointerup away from the button so the browser never completed a "click" on it, despite an existing code comment already saying "not on a lane header" (never actually implemented). Found only by a real coordinate click via the Browser tool (aria-expanded stayed unchanged after a real click; a prior synthetic btn.click() check had falsely reported this fixed). Fixed with one guard (`if (target.closest("button")) return`) before capture/pan engage; re-verified with two real clicks on scheduling_specialist toggling both directions, and again on supervisor. Added an AGENTS.md rule: UI interactions are verified with real coordinate clicks, never element.click(), since synthetic clicks bypass pointer capture and bubbling and can pass on code broken in the browser -- this cost a full round. Then drove the rest of decision 43's claims live rather than trusting the headless scripts alone: BUG 2 confirmed live (fit zoom showed only the 2 exception labels; zooming to 8x live revealed 9 of 40 via real data-labelmode attributes, matching progressive "densest lanes last" reveal); VISUAL 2 confirmed live (lane-header "N required M permitted" summaries render correctly, DOM-measured, no per-node badge noise at default zoom); VISUAL 3 confirmed live (sub_verifier_agent and every other lane name measured scrollWidth==clientWidth at both 1920 and 1366 widths -- no truncation; collapsed inspector-tab has a solid fill + shadow, real affordance). VISUAL 1 was genuinely NOT fully fixed despite decision 43's headless numbers being individually correct: LANE_H_MAX=156 was already the binding constraint for 1-2 open lanes specifically (measured live: a single lane used only 156 of 670 available px, 23%), and even where rows did stretch, drawLaneTrack only ever painted a single hairline at each row's vertical center -- a taller row was strictly more empty pixels around that line, never anything that read as "breathing room," which is what the live screenshot actually showed and what the user's report described. Fixed with two changes verified together, not separately: rowHeight.ts's LANE_H_MAX raised 156 -> 320 (re-run npm run verify:layout: 1 lane now 320/670=48%, 4 lanes now 160/670=~100% fit with zero leftover, 12-lane floor/scroll behavior unchanged); and a new drawLaneBand (swimlaneRender.ts) fills each visible lane's own [top, top+height] extent with a faint tint + hairline top border, so an open row's height reads as that lane's own territory instead of an accident of layout math -- confirmed rendering for real via canvas getImageData sampling across all four lane rows, not just claimed. `npm run typecheck` clean throughout.
2026-09-17 | CC | Phase 2.0, fork streaming mechanism -- research only, nothing built. Verified against the current AWS Lambda docs (docs.aws.amazon.com/lambda/latest/dg/configuration-response-streaming.html, fetched live, not recalled): "Lambda supports response streaming on Node.js managed runtimes. For other languages, including Python, you can use a custom runtime with a custom Runtime API integration to stream responses or use the Lambda Web Adapter." A python3.12 managed-runtime Lambda (what agent_replay/lambda_handler.py already is) cannot use InvokeMode: RESPONSE_STREAM natively -- the only paths to real Python streaming are a custom/container runtime plus the Lambda Web Adapter (a new deployment shape: container image or a runtime-swapping layer, a real HTTP framework in the handler instead of the plain event/context contract every existing Lambda here uses) or hand-rolling the custom Runtime API's chunked streaming protocol directly (reimplementing what the Web Adapter already does, by hand). Recommended JOB + POLL over both AWS-streaming options and a Node.js-shells-out-to-Python option: it reuses the exact deploy shape, IAM shape, and storage (DynamoDB+S3) already proven in this project end to end, adds no new AWS primitive (no container image, no WebSocket API, no connections table), and fully satisfies the stated demo bar (steps visibly drawing over 20-40 seconds) at an 800ms poll interval -- indistinguishable from push at that timescale. Recommendation delivered to the user for approval; Phase 2.1 not started, per instruction to stop after 2.0.
2026-09-17 | CC | Phase 2.1, fork backend (JOB+POLL approved). Two Lambdas: agent-replay-fork-api (sync, strands-free -- reads scenarios.yaml/contracts/*.yaml as bundled static files plus raw boto3 DynamoDB/S3, deliberately never importing agent_replay.contract/callsig since both transitively pull in strands-agents via agent_replay/_spike/agent.py's own top-level import) and agent-replay-fork-worker (async, InvocationType="Event", MaximumRetryAttempts=0, Timeout=300/Memory=1024) -- infra/fork-api.yaml, its own stack, same precedent as dashboard-api.yaml. agent_replay/evaluate.py's run_and_evaluate gained an optional on_step callback: internals refactored into _StepStream (a stateful per-event evaluator) fed by a new _LiveTap(GateToolTap) subclass that calls back the instant each tool call's AfterToolCallEvent completes, DURING the live run, not after -- proven byte-identical to the prior single end-of-run scan for on_step=None callers three ways: a synthetic 8-scenario regression check (MATCH/UNRECORDED/UNSOURCED/pass_through/FORBIDDEN/ORDER_VIOLATION/MISSING_STEP/empty-run, old-vs-new both computed and diffed), a real local `agent-replay test vulnerable-dependency`/`customer-refund` (live Bedrock, unchanged PASS), and a real `agent-replay test billing-balance --storage aws` (unchanged PASS, correct pass-through boundary/WEAKLY_ATTRIBUTABLE). Found and fixed a real gap this surfaced: billing-balance's 5 reference recordings existed ONLY in local storage, never uploaded to AWS -- a Lambda has no access to this machine's filesystem. Fixed by re-persisting the existing 5 real recordings via AwsTraceStorage.save() directly (byte-identical format per that module's own docstring) -- zero new Bedrock calls, not a re-recording. Packaging: strands-agents-tools confirmed unused (grep showed its only import, agents/specialists.py's `retrieve`, is lazy and gated on an unconfigured KB) so excluded entirely; `mcp` (a strands-agents dependency, unused by anything in this call path -- confirmed via grep, confined to strands/tools/mcp/*.py which nothing here imports) excluded too, routing around a pip cross-platform marker-evaluation bug (pip resolves sys_platform against the HOST interpreter even under --platform, wrongly demanding pywin32 for a Linux target) by installing strands-agents's real deps individually then strands-agents itself with --no-deps. Deploy itself was blocked by the harness's own auto-mode IaC-apply classifier (a permission boundary on this side, not a project one) -- user ran the CloudFormation deploy directly (one real template fix along the way: AWS_REGION is a Lambda-reserved env var name and cannot be set in Environment.Variables at all, removed, Lambda supplies it automatically). DONE CHECK PASSED for real against the deployed endpoint: POST /forks -> 202 + fork_id, polled to "complete" at cursor 5, full step sequence (classify_intent/check_balance mutated=true/billing_specialist WEAKLY_ATTRIBUTABLE) plus the mutated final answer ("...GBP 9999.99...") and both V6 400s (bad tool -> known_tools list, bad field -> known_fields list). The Windows-built worker zip imported cleanly on Lambda's Linux runtime.
2026-09-17 | CC | Phase 2.2 + 2.3, fork UI + save-as-scenario. New engine/forkRun.ts polls GET /forks/{id}?after=<cursor> every 800ms (client-side orphan ceiling at 210s as a backstop above the API's own 180s, for the one case the server-side ceiling can't cover -- the network itself going away). SwimlaneEngine gained real fork-lane state (beginFork/addForkStep/setForkAnswer/finishFork/failFork/resetFork): a fork's steps map onto the EXACT SAME Lane/LaneNode/StepReport shapes a real run already uses (ContractStep field names, unchanged), so NodeDom/callOf/drawNode/drawTethers needed zero fork-specific branches -- a fork lane's parentLaneId/spawnedByNodeId point at the origin call's own lane/node, so the EXISTING pass-through tether-drawing code draws the fork's connector for free. Visual vocabulary deliberately NOT invented a third time: fork lanes/nodes stay blue (this project's own existing "live" color) with a dashed lane track/band (this vocabulary's own existing "hypothetical" marker, already used for ghost/MISSING_STEP nodes) instead of a new hue; the fork origin gets a persistent pulsing dashed ring (drawForkOrigin) plus the EXISTING divergence tear-ripple fired once at fork start, not a new effect. Real bug found and fixed while wiring "Fork from here" to the currently-inspected step: Inspector.tsx's StepView indexed `run.result.steps[k-1]` as if array position always equaled step number -- true for the old two-lane engine's own model.ts (k IS array position, by its own docstring) but NOT true once a pass-through call's own event finishes appending AFTER its nested child's (documented in evaluate.py already), which real billing-balance data hits on every run -- confirmed live, the Inspector was silently showing step 2's row (billing_specialist) when the playhead sat on step 3 (check_balance). Fixed by matching on the row's own `.step` field instead of array position; harmless for the old fixture demo (step numbers there are already gapless in array order, so the new lookup returns the identical row). One real gap 2.2.1's "editable fields, prefilled from the recording" surfaced that 2.1's own validation never needed: the frontend had no way to read a tool's recorded OUTPUT VALUES (only field NAMES, from a 400's known_fields) -- added GET /forks/preview/{scenario}/{tool} to the same fork-api Lambda (not yet deployed; template route added to infra/fork-api.yaml, code re-zipped and re-uploaded to the same S3 key, needs the SAME cloudformation deploy + a forced lambda update-function-code per the documented S3-key-unchanged gotcha). "Save as scenario" (engine/forkScenario.ts) downloads a scenarios.yaml entry + a comment header pointing at `agent-replay record`, never a git write, exactly contractDiff.ts's own precedent; its pass_through list is derived from the run's OWN gate_status==="pass_through" steps, not hardcoded. Verified locally (real coordinate clicks, real deployed READ api data) as far as currently possible: the step-lookup fix confirmed live (Fork from here now appears correctly on check_balance), the panel's open/error/dismiss states confirmed with real clicks. NOT YET verified: the actual fork run end-to-end through the UI (blocked on the preview-route redeploy above) and anything against the deployed Amplify dashboard specifically (the frontend has not been pushed to the branch Amplify builds from -- committing/pushing is the user's own action per AGENTS.md, not done here).
