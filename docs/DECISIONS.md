# Decisions log

Append one line per decision. Never edit or delete existing lines.
Format: YYYY-MM-DD | initials | decision

2026-09-13 | PS | Spike scope frozen: prove model-response interception and injection only.
2026-09-13 | PS | Bedrock preflight passed. ap-south-1 requires inference profile ID apac.amazon.nova-lite-v1:0; bare model ID rejected with ValidationException.
2026-09-13 | PS | Observed Nova at temperature 0 return "Preflight OK" when asked for exactly "preflight ok". Confirms hosted inference is not reproducible. Injection is the only defensible determinism claim.
2026-09-13 | CC | Spike PASSED via RULE B: BedrockModel subclass overrides stream() to yield recorded stream events, tools replayed via public BeforeToolCallEvent.selected_tool hook; replay hash matched recording with 0 Bedrock HTTP sends and 0 tool-body executions (no hook can write a model response, so RULE A was not available).
2026-09-13 | CC | Gate replay added (spike/gate.py): tools injected from golden trace by name+args, model LIVE with changed prompt/model ID. Verified: unchanged config still FAILs on DIFFERENT_ANSWER (Nova non-determinism, not a bug); prompt bias toward skipping lookup gives ATTRIBUTABLE DIFFERENT_TOOL at step 1 with zero unrecorded calls (order-independent matching absorbed the reorder, so --strict had nothing to halt on); tone-only change gives ATTRIBUTABLE DIFFERENT_ANSWER only, no tool-level divergence; a genuinely novel tool call correctly halts under --strict and is UNRECORDED/UNATTRIBUTED otherwise.
2026-09-13 | CC | Gate replay verdict split into PASS/WARN/FAIL: trajectory divergence (DIFFERENT_TOOL/ARGS/EXTRA_STEP/MISSING_STEP/UNRECORDED) is FAIL exit 1; a matched trajectory with only DIFFERENT_ANSWER is WARN exit 0 by default (--fail-on-answer promotes it to FAIL), since Nova wording is not reproducible even with nothing changed. Re-verified: 3x case A all WARN/exit 0; case B/D FAIL DIFFERENT_TOOL step 1; case C WARN; unrecorded-call extras unchanged (FAIL/UNRECORDED and strict halt).
2026-09-13 | CC | Phase 1 probe: Nova via Strands returned parseable JSON with exact expected keys 5/5 runs when asked to end response with a fixed-shape JSON object. Mutation replay added (--mutate TOOL:JSON, repeatable, shallow-merged into the recorded tool result before injection, validated against golden tool names before anything runs); step table marks the mutated step MUTATED. Answer-tier removed: verdict is trajectory-only PASS/FAIL, final-answer text is informational (matched boolean + generic JSON diff if both parse), --fail-on-answer opts back into strict text matching. Re-verified A/B/C/A-2002 unchanged in outcome; M1 (refund_eligible true->false) correctly produced MISSING_STEP/ATTRIBUTABLE (agent skipped the refund); M2 cosmetic mutation PASSed; M3 confirmed prompt+mutation both named in the report.
2026-09-13 | CC | Toy agent replaced for full-replay proof purposes with spike/audit_agent.py (new file, spike/agent.py untouched): dependency security auditor hitting real pypi.org/api.osv.dev, requests library (already transitive via strands-agents-tools, no new dependency). Golden trace traces/audit-001.json: 11 events, 66.2KB. Full replay PASSed with 0 real tool executions and 0 non-loopback socket ops -- the first time that network-zero count is proven against tools that would genuinely reach the internet. Gate replay against this agent surfaced two real findings unrelated to any prompt change: the model checks independent packages in a different order each run, which the positional comparator reports as false-positive DIFFERENT_ARGS/DIFFERENT_TOOL; and it intermittently mangled a long backslash-heavy Windows path argument. Required a minimal, flagged gate.py change: --agent-module for dynamic agent selection, and _apply_mutation now detects JSON tool-output text (audit_agent) vs the toy agent's key=value text before merging, since --mutate could not otherwise touch a JSON-shaped tool result.
2026-09-14 | CC | Comparator rewritten from positional to dependency-aware (spike/gate_compare.py): happens-before edges from substring-matching argument values against prior tool outputs; PASS requires same multiset of calls AND edge order preserved both ways. Retired DIFFERENT_TOOL/DIFFERENT_ARGS/EXTRA_STEP (no longer meaningful without positions); added ORDER_VIOLATION (edge broken, unit-verified both directions) and UNSOURCED_ARGUMENT (unrecorded call whose args trace to no prior output or prompt -- takes precedence over UNRECORDED). Toy agent: A/M2 PASS, B/M1 FAIL correctly (B mostly via MISSING_STEP -- the biased agent omits lookup_order rather than reversing it; ORDER_VIOLATION confirmed working via direct synthetic test). Audit agent: full replay still PASS/0-network; gate-replay-unchanged FAILed 5/5 for two real, non-comparator-bug reasons -- a false happens-before edge from substring name overlap ("strands-agents" is literally a substring of "strands-agents-tools"), and boto3 being unpinned in requirements.txt so the model fabricates a plausible version every run (even golden's own recording does this) -- both explained in chat, neither papered over. Also demonstrated a real permissiveness gap: a candidate that reads refund_eligible=false but calls issue_refund anyway (order_id never encodes that field) gets a clean PASS -- the comparator verifies data availability, not decision correctness. Fixed audit_agent target to a file:// URL (was a backslash Windows path Nova intermittently mangled) and added alphabetical-order processing to its system prompt (demo-stability only, not a fix).
2026-09-14 | CC | Three targeted fixes: (1) edge detection changed from substring to exact-token matching (regex split, hyphens not a separator) -- fixes the strands-agents/strands-agents-tools false edge, verified ORDER_VIOLATION still fires on the toy agent's synthetic reversed-order trace and PASS still holds for correct order; (2) boto3 pinned to 1.43.93 (matches pip freeze) and traces/audit-001.json re-recorded -- golden is now internally consistent, every check_vulnerabilities version traces to get_package_info or the manifest; (3) gate.py always prints golden vs candidate final-answer text (informational, does not affect verdict) -- the mutation verdict flip SAFE TO DEPLOY -> BLOCKED is now directly readable in the report. Toy agent (A/B/M1/M2) all verified correct. Audit agent full replay PASS/0-network. Gate-replay-unchanged still FAILed 5/5 for a third, distinct, real cause found after fixes 1+2 landed: golden's own trajectory calls get_package_info(boto3) and get_package_info(strands-agents) AFTER already using their versions (redundant/habitual confirmation), and check_vulnerabilities echoes its own name/version arguments back in its output, so golden's own idiosyncratic ordering is read as a required edge that a more natural verify-before-check ordering violates. Not fixed per instruction (no third comparator redesign); reported to the user for a decision. Added docs/LIMITATIONS.md.
2026-09-14 | CC | Final comparator fix: a token echoed from a call's own input arguments is excluded from that call's output-as-source only if the token ALSO already appeared in an earlier call's output this run; a token echoed from input that has no earlier occurrence is the call's own primary subject (e.g. an id the caller supplies) and still anchors an edge. Fixes the audit agent's reversed false edges (check_vulnerabilities echoing name/version, wrongly appearing to precede get_package_info) while preserving the toy agent's lookup_order->issue_refund edge (order_id has no earlier occurrence, since lookup_order runs first) -- verified via both the synthetic reversed-order (FAIL/ORDER_VIOLATION) and correct-order (PASS) traces. Golden's remaining edge set (7 edges) confirmed all genuine by inspection. Re-verified toy agent A/B/M1/M2 all correct. Audit agent: full replay PASS/0-network; gate-replay-unchanged now 4/5 PASS, 1/5 FAIL -- the one failure is real live-model variance (an unprompted extra tool call), not a comparator artifact, documented in docs/LIMITATIONS.md with the measured ~20% rate; per instruction, not treated as something to fix further. Loosened-prompt run now shows the real regression cleanly: MISSING_STEP, fully ATTRIBUTABLE, candidate's own text explains it skipped strands-agents-tools by "judgement." Mutation clean->CRITICAL: trajectory PASS (actions identical, only injected data differed), verdict flip SAFE TO DEPLOY -> BLOCKED directly readable in the report. Comparator work stops here per instruction; moving to the storage layer next.
2026-09-14 | CC | AWS storage backend added: spike/storage.py (TraceStorage ABC, LocalTraceStorage unchanged-behavior wrapper, AwsTraceStorage). DynamoDB table "agent-replay" (pk run_id, sk sequence_number, on-demand) holds only small per-event fields; S3 (CloudFormation-auto-named bucket, no account ID in the name, 7-day expiry lifecycle rule) holds every event payload content-addressed by SHA-256, reusing the hashes replay already computes. Deployed via infra/template.yaml as stack agent-replay-dev in ap-south-1 (required the user to grant cloudformation:* to CLI_Agent_Replay, which had none). Load phase (S3/DynamoDB fetch) is structurally separated from run phase in replay.py/replay_audit.py, with counters snapshotted at the boundary; verified full replay from --storage aws still PASSes with 0 real executions and 0 non-loopback sockets during the run phase specifically (load phase alone used 6, expected and excluded by design). Recording writes are buffered in memory and flushed after the run completes (not blocking the agent loop): measured latency 1.5ms local vs 3252.4ms AWS for an 11-12 event audit trace. Dedup measured by recording the same audit run twice: 1/22 payloads deduped (read_manifest's input URL, the only payload structurally guaranteed identical across two live runs -- model text is never identical and tool outputs vary run to run). --storage local confirmed unchanged on record/replay/gate. Stack left running per instruction; teardown requires emptying the S3 bucket before stack delete (CloudFormation cannot delete a non-empty bucket).
2026-09-14 | CC | Two fixes + perf change to AWS storage. (1) S3 keys now golden/<hash>.json vs runs/<hash>.json; lifecycle rule ExpireNonGoldenTraces scoped to Prefix runs/ only, so a golden trace never expires while DynamoDB still lists it. record_audit.py --golden sets run_kind=golden. Deleted 45 orphaned objects and their 2 stale DynamoDB run items from the old flat events/ key layout before the stack update, per instruction not to leave unowned objects. (2) Metadata item now stores prompt_hash/final_answer_hash only; prompt and final_answer are content-addressed in S3 like every other payload, fetched during the (already network-bound, proof-exempt) load phase. (3) S3 head+put calls parallelized via a bounded ThreadPoolExecutor(8); DynamoDB batch_writer left as-is. Verified: golden lands under golden/, normal run under runs/; full replay from AWS golden PASSes with 0 real executions and 0 non-loopback sockets during the run phase; gate replay from AWS golden PASSes; --storage local unchanged on record/replay/gate. Write latency ~2030-2037ms for an 11-event trace, down from 3252ms for a 12-event trace (~1.5x per-event improvement). Added a Scaling boundaries section to docs/LIMITATIONS.md documenting (not fixing) unpaginated query, no secondary index, no optimistic locking, and per-init describe_stacks cost, plus the dedup-is-a-measurement finding.
2026-09-14 | CC | Packaged as installable agent-replay (pip install -e ., pyproject.toml; spike/ left completely unmodified, agent_replay/paths.py puts it on sys.path exactly as running "python spike/gate.py" already does): agent-replay init/record/test/replay/gate, contract derivation as exact set logic (requires/permits/order reusing gate_compare._build_edges unchanged, aggregated to tool-name granularity) written to agent-replay/contracts/<scenario>.yaml in a hand-formatted tool(arg=value) call-signature format (not yaml.dump) so a contract diff reads in seconds, plus deterministic forbids rules (when/count/count-per-arg, no LLM judgement) evaluated against the exact tool results injected during a run. Verified: vulnerable-dependency (audit_agent, 3 runs) derived requires=7/permits=0/order=3, then agent-replay test PASSed 5/5 exit 0 against live Bedrock; adding "issue_refund when lookup_order.refund_eligible == false" to the toy agents customer-refund contract correctly FAILed as FORBIDDEN (not MISSING_STEP) once the agent was made to call issue_refund anyway, and still PASSed unmutated; loosening the audit agents prompt to skip two packages correctly FAILed on MISSING_STEP with a 6-line contract diff; --json and --format junit both emit valid parseable output; confirmed zero boto3.client() calls and zero real tool-body executions during --storage local runs (only Bedrock + PyPI/OSV network activity, from the model and the reference recordings own tool bodies at record time). New dependency: PyYAML (already installed transitively via strands-agents-tools) for scenarios.yaml and contracts/*.yaml; arg parsing stayed stdlib argparse, no new dependency there.
2026-09-14 | CC | Phase 1 portability: added agent_replay/portable.py (collapse-at-capture, expand-at-use, collapse-at-comparison) so a scenario prompt is authored with a {repo} token, expanded to a real absolute path only for the live agent call and for widening a portable reference recording back into gate.py GateToolTap (unchanged, needs real args to exact-match), and collapsed back to {repo} before anything is persisted or before a live candidates args are checked against contract keys. Migrated the 5 existing on-disk traces and re-derived both contracts in place (pure data transform, zero Bedrock/PyPI calls, no re-recording) -- this incidentally exposed and required restoring a real bug: contract.derive() always resets forbids to [], silently destroying the hand-authored customer-refund forbids rule from the prior session; restored it, left the underlying bug undocumented-but-real for the final report since fixing derive-vs-human-edits was out of scope for this phase. Verified end-to-end with a real Bedrock call: copied the repo (agent_replay/, spike/, agent-replay/, traces/, pyproject.toml) to a fresh checkout on a different drive letter, fresh venv, fresh pip install -e ., and agent-replay test vulnerable-dependency PASSed against the copied traces, exit 0. Confirmed --replay (byte-identical mode) still works on the SAME machine after migration by expanding the whole loaded trace back to absolute at replay time, but explicitly does NOT claim --replay is portable across machines (the recorded model conversation itself is never rewritten -- see LIMITATIONS.md). Grepped the full repo for drive letters/absolute paths: none remain outside illustrative comments/docstrings and .venv.
2026-09-14 | CC | Phase 2: recorded vulnerable-dependency with --runs 5 (the one authorized recording this session) to test whether the multi-run envelope earns its place. permits stayed empty at N=5, identical in shape to N=3 -- requires=7/order=3 both times. Root cause: audit_agents system prompt and the pinned, unvarying manifest leave the model zero genuine discretion (every pinned package always gets checked with the same version), so N runs currently buy confidence that requires is stable, not visible new information; historically the order fields cross-run intersection DID matter (per this logs prior single-golden-trace comparator bugs from one runs idiosyncratic ordering). Proposed but did NOT record (per instruction to stop and wait) a mixed-manifest scenario (2 pinned + 2 unpinned packages, unchanged agent/prompt) designed to expose real run-to-run variance in permits, since the existing scenario structurally cannot. Reported to the user for a decision before recording anything further.
2026-09-14 | CC | Fixed the forbids-clobber bug: contract.derive() now takes existing_forbids and carries it forward instead of resetting to [] on every re-derivation; added contract.forbids_warnings() to flag (not drop) a preserved rule whose tool no longer appears in the new recording. cmd_record now reads the on-disk contracts forbids before overwriting it. Verified: added a rule, re-derived vulnerable-dependency from its 5 existing traces with no re-recording, rule survived; added a rule naming a nonexistent ghost_tool, re-derived again, rule was KEPT and the expected warning fired naming ghost_tool; both test rules removed afterward. Phase 2 resolved per instruction: predicted requests/pyyaml permits behavior before recording a new vulnerable-dependency-mixed scenario (audit_agent unchanged, one new fixture with 2 pinned + 2 unpinned packages, 5 runs, the second and final authorized recording); permits populated (10 entries) validating the feature, but two specific predictions were wrong (pyyaml landed in requires at 5/5 -- MORE consistent than requests, the opposite ranking predicted; strands-agents, a PINNED package, unexpectedly landed in permits at 3-4/5 rather than requires, apparently ordinary live-model variance rather than genuine discretion) -- reported plainly rather than reinterpreted. Decision applied: keep --runs N and the README claim, documented with this real (populated, imperfect) example instead of the all-empty one, explicitly not described as a headline feature; also surfaced and documented two unplanned findings (exact-match keying treats "pyyaml" and "PyYAML" as different entries; the model sometimes emits the literal string "latest" as a version value) and one real instability (a fresh agent-replay test against the mixed contract can still FAIL on a pyyaml version string none of the 5 references produced -- an honest limitation of finite N on an unbounded-variation input, not a bug).
2026-09-14 | CC | Phase 3: added 4 docs/LIMITATIONS.md sections -- order-edge granularity is tool-name level not per-instance (concrete uncaught-regression example given: check_vulnerabilities(boto3) with no prior get_package_info(boto3) passes order if ANY get_package_info happened earlier this run, since evaluate.py order_seen tracks tool names only); the callsig tool(arg=value) formats numeric-string coercion caveat plus a newly-observed related gap (exact-match keying treats pyyaml vs PyYAML as different entries, and a live model was observed emitting the literal string "latest" as a version value); editable-install-only (pip install agent-replay from PyPI does not work, agent_replay/paths.py requires its own parent directory to be the actual repo checkout); and two carried-forward findings from phases 1-2 -- replay byte-identical mode is verified same-machine only, never claimed cross-machine, and a fresh CI git clone has zero local trace files since traces/ is gitignored, so CI must use --storage aws. Confirmed README never claims a PyPI install works.
2026-09-14 | CC | Phase 4: added .github/workflows/agent-replay.yml (pull_request not pull_request_target, reasoning documented inline: scenarios.yaml names an arbitrary agent module this workflow imports and runs, so a fork PR must not get this workflows AWS trust), .github/scripts/render_pr_comment.py (standalone, reads agent-replay test --json output, renders scenario-first PASS/FAIL markdown with a plain-English divergence sentence per cause, counters, attribution, a dashboard link marked not-live-yet) and post_pr_comment.sh (finds and PATCHes the existing marker comment via gh api instead of posting new ones), and infra/github-oidc.yaml (a SEPARATE template from infra/template.yaml since an AWS account permits only one OIDC provider per issuer URL -- CreateOidcProvider is a parameter precisely so an account that already trusts GitHub Actions elsewhere does not fail to deploy this). Fixed a real CLI gap this surfaced: --json and --format junit were mutually exclusive, but the workflow needs both from one live run (two separate test invocations would burn two real Bedrock calls and could disagree) -- now combinable, JUnit defaults to a real file instead of stdout when JSON already owns stdout. Added a tool_bodies counter to ContractResult (real tool executions), missing before, needed for the PR comments Counters line. Verified: ran the actual command sequence locally (pip install -e ., agent-replay test --json --format junit, the renderer) substituting --storage local for aws since no further recording was authorized -- documented as a substitution, not hidden; rendered and printed both a real PASS comment (3 scenarios) and a real FAIL comment (customer-refund FORBIDDEN, from the existing gate mutation) verbatim; added an explicit guard for the crashed-with-zero-results case, since all([]) is vacuously true; fixed a real bug the validation surfaced, bare `on:` parsing as the YAML 1.1 boolean True under plain PyYAML (GitHub Actions itself is unaffected, quoted it anyway); validated infra/github-oidc.yaml with the real, read-only aws cloudformation validate-template (not a deploy) -- confirmed syntactically valid, correctly reports CAPABILITY_NAMED_IAM required, matching the documented deploy command; confirmed JUnit output is well-formed XML. Unverified, stated plainly: real OIDC token exchange, the gh api PR-comment update path, upload-artifact, and fork-PR credential denial all require an actual GitHub Actions run against a real PR and are not simulable locally.
2026-09-14 | CC | Phase 5: added agent_replay/ci.py (reads GITHUB_SHA/HEAD_REF or REF_NAME/ACTOR, plus pr_number parsed from GITHUB_EVENT_PATH JSON when the event is pull_request; every field None when absent). Extended spike/storage.py AwsTraceStorage.save()s run_metadata item with scenario, contract_hash, branch, pr_number, triggered_by (explicit DynamoDB NULL via boto3 when absent) and commit_sha (the ONE deliberate exception -- OMITTED, not NULL, when absent, since it backs a new GSI and a GSI key attribute cannot be NULL; DynamoDBs sparse-index behavior then correctly excludes local/non-CI runs from that index rather than erroring). Restructured cli.py cmd_record into three phases (record all N runs in memory, derive+hash the contract once, THEN save each run tagged with that contract_hash) since contract_hash cannot be known until every run has been made. infra/template.yaml: added a CommitIndex GSI (commit_sha hash key, created_at range key, INCLUDE projection) -- shown as a real diff, validated syntactically via the real read-only aws cloudformation validate-template (no CAPABILITY_* needed, matches the templates own no-IAM description), update command given (aws cloudformation deploy --template-file infra/template.yaml --stack-name agent-replay-dev --region ap-south-1), NOT run. Verified metadata population by patching AwsTraceStorage S3/DynamoDB calls to in-memory fakes so the real unmodified save() code ran with zero network calls and zero cost: local (no CI vars) correctly omits commit_sha and NULLs branch/pr_number/triggered_by; a simulated pull_request CI environment correctly populates all fields including pr_number parsed from a fake event payload. Explicitly unverified and stated as such: the GSIs actual Query behavior, which requires deploying it.
2026-09-14 | CC | Phase 6 final audit: fresh-clone simulation (file copy, deliberately WITHOUT traces/ since it is gitignored and nothing this session is committed) confirmed pip install -e . works standalone, but reproduced a real, previously-only-theorized gap for real: agent-replay test vulnerable-dependency on a truly fresh checkout fails with "could not load reference recording" because the committed contract has no local trace data to inject from -- fixed by adding an explicit callout to READMEs Quickstart rather than leaving it to be discovered. Interactive agent-replay init verified end-to-end via piped stdin (zero cost, answered no to recording). Re-ran the full verification suite: agent-replay test 5/5 PASS exit 0; full replay of vulnerable-dependency--1 confirmed 0 non-loopback socket ops, 0 Bedrock HTTP sends, 0 real tool executions during the run phase, answer sha matched; the customer-refund forbids rule fired FORBIDDEN correctly on the mutated run and stayed PASS unmutated; --json + --format junit together produced valid JSON and well-formed XML from one live run. Closed a real verification gap found during this audit: ORDER_VIOLATION had never actually fired in the new contract-based evaluator this session (only inferred by reading the code) -- fired it for real via a reversed-order-biased gate prompt on customer-refund, correctly FAILed at step 1 with 0 real tool executions. Full ranked list of what remains broken, unproven, or unfinished is in the final report delivered in chat, not repeated here.
2026-09-14 | CC | Proved --storage aws against the real deployed agent-replay-dev stack. Recorded vulnerable-dependency (5 runs, authorized) to AWS; confirmed via direct dynamodb get-item and s3 list-objects that items/objects land under the right table and golden/ prefix with the new Phase-5 metadata fields present and CI fields correctly NULL/omitted for a local invocation. agent-replay test/replay/five-times-test --storage aws all PASSed, exit 0. Found and fixed two real bugs on first contact, not papered over: (1) cmd_replay never actually checked spike/agent.py COUNTS during its run phase -- it could report PASS without verifying the zero-network claim at all; added the same load/run-phase boundary spike/replay.py already used, now verified 0 sockets/0 bedrock/0 tool-bodies for real against both backends. (2) agent-replay test with no scenario arg (exactly what CI runs) discarded EVERY already-computed result the moment any one scenario could not be evaluated -- first via an uncaught SystemExit from a missing AWS recording, then via an uncaught live-model exception (a real Bedrock modelStreamErrorException hit during testing) -- both silently rendering as a misleading overall PASS once results was empty and all([]) has vacuous truth. Fixed by containing both failure classes per-scenario (ReferenceLoadError, and a broad catch around run_and_evaluate), threading a new verdict=ERROR entry through report.py/render_pr_comment.py so a scenario that never ran renders distinctly (warning icon) and correctly forces the overall comment to FAIL, not PASS by omission. Deployed the CommitIndex GSI (infra/template.yaml only, as authorized); proved it for real: wrote a throwaway item with commit_sha set (real DynamoDB+S3, fabricated trace content, zero Bedrock cost) and queried CommitIndex directly, got back exactly that one item with the right projected attributes; cleaned up the probe item and its S3 objects afterward. Resolved the s3:HeadObject question definitively (it is not a real IAM action; HeadObject is authorized by s3:GetObject, which the policy already grants separately) and tightened infra/github-oidc.yaml to the exact action set spike/storage.py actually calls (dropped unused dynamodb:GetItem/PutItem, dropped the non-functional s3:HeadObject) -- still not deployed. Full recommendation on committing traces/ vs AWS-only vs a hybrid delivered to the user in chat, not implemented pending approval.
2026-09-14 | CC | Phase 1 fixes. (1.1) Goldens hybrid: .gitignore now excepts the 7 curated reference traces for vulnerable-dependency/customer-refund (git add -f staged, not committed), README documents the trade-off (contract is the reviewable artifact, traces churn on every re-record so only the two stable demo scenarios are committed); verified via a from-scratch fresh-clone simulation built from exactly `git ls-files --cached` + `--others --exclude-standard` (what a real commit right now would contain) -- fresh venv, fresh install, agent-replay test vulnerable-dependency/customer-refund both PASS with zero AWS and zero manual trace copying. (1.2) Transient Bedrock errors: added _is_transient()/_run_scenario() (cli.py) -- retries once on a transient-looking exception, only reports ERROR if the retry ALSO fails, with a transient flag that is False if the retry fails with a DIFFERENT (non-transient-looking) error. Threaded through everywhere a human reads it: the CLI (_print_error, a distinct ERROR banner, separate from PASS/FAIL), report.py (JSON transient:bool, JUnit <error type=TransientError> vs ReferenceLoadError), and the PR comment (a distinct icon/label and explanation, plus the top banner reads FAIL (transient only) when every non-pass is transient). Verified by injection: a fake exception matching a transient marker retries exactly once and, if it fails again with an unrelated error, is correctly NOT marked transient; if it fails again with the SAME transient signature, IS marked transient; a non-transient error never retries; a transient error that succeeds on retry surfaces no error at all. (1.3) Audited every claim the CLI/JSON/JUnit/PR comment makes: all but two are directly read from data computed moments earlier (GateToolTap/ReplayableBedrockModel/ToolTap counters, the just-written contract text, the just-completed Contract objects) -- no unchecked assertions found beyond the cmd_replay zero-network gap already fixed last session. Found and fixed one real omission (not a false claim, a silent drop): ContractStep never carried gate_mutated at all, so a --mutate run showed no indication of which step was altered, unlike the original gate.py which always did -- added the field, verified a real --mutate run now marks the mutated step MUTATED in the CLI table and true in JSON.
2026-09-14 | CC | Phase 2 dashboard reads real runs. Added agent_replay/dashboard.py (maps evaluate.py's ContractResult onto the web app's existing GateRun/ComparisonResult shape unchanged, per instruction -- no new fields invented) and wired cmd_test/cmd_gate to persist every --storage aws run via spike/storage.py's new save_test_run/save_comparison/record_acceptance. Wrote agent_replay/lambda_handler.py self-contained (boto3+stdlib only, deliberately NOT importing spike/storage.py to avoid pulling in the full strands-agents SDK) implementing GET /runs/{id}, GET /commits/{sha} (CommitIndex GSI query), GET /scenarios, POST /runs/{id}/accept, plus infra/dashboard-api.yaml (API Gateway HTTP API + Lambda + role). Deploy BLOCKED: CLI_Agent_Replay lacks apigateway:POST and iam:DeleteRolePolicy, leaving stack agent-replay-dashboard-api stuck ROLLBACK_FAILED -- reported verbatim, not self-remediated. Built agent_replay/dev_api_server.py (stdlib http.server routing into the exact same lambda_handler.handler()) as a verification stand-in against real AWS data while deployment is blocked. Also fixed a real GSI schema gap found by querying live: CommitIndex's projection never included verdict/accepted_by/accepted_at (predates those fields) -- required a two-step CloudFormation deploy (DynamoDB forbids updating a GSI's projection in place) which WAS carried out on the real dev stack, verified via a live query. Web: added api.ts/router.tsx, /commit/:sha (triage rail) and /run/:runId routes, RunView.tsx (reuses Engine/ForkOverlay/NodeCard/Timeline/Hero unchanged -- "data-source change, not a redesign"), loading/error states (StateScreen.tsx). PR comment's "View the fork" now links to /commit/{sha}. Verified live end-to-end against real --storage aws data (a real PASS and a real FAIL produced via simulated CI env vars over the existing authorized reference pool, no new recording): triage rail, fork, Inspector, and scrubbing on a real (larger) trace all confirmed working in-browser. Fixed one real layout bug found during that verification: RunView's root reuses the fixture demo's .stage class (position: fixed), which painted over the triage rail regardless of CommitPage's flex layout -- added a .stage-embedded override (position: absolute within .triage-main's position: relative) rather than forking the component.
2026-09-14 | CC | Phase 3 accept in the dashboard. Added web/src/engine/contractDiff.ts (buildContractDiff maps MISSING_STEP/UNRECORDED/UNSOURCED_ARGUMENT to a single acceptable requires/permits line each; ORDER_VIOLATION/FORBIDDEN are shown for context but marked non-acceptable -- neither reduces to one line) and DiffPanel.tsx (per-line Accept toggle, no accept-all anywhere, only visible on FAIL). Chose a downloadable printed patch over a full contract file: the dashboard only ever fetches the comparison RESULT, never the contract's full YAML text, so it cannot honestly offer a drop-in replacement -- buildPatch() renders +/- lines in callsig.py's own tool(arg=value) format, small enough to review in seconds and apply by hand, matching this project's own stated philosophy for the contract file itself. Accepting calls POST /runs/{id}/accept (spike/storage.py's record_acceptance), writing accepted_by/accepted_at into the run's DynamoDB item -- confirmed by direct get-item. Verified end-to-end on the real FAIL run from Phase 2 (fade111, MISSING_STEP on strands-agents-tools at two steps): accepted both lines in the UI (one Accept click each, confirming the no-bulk-accept design holds under a real multi-change diff), confirmed the accept API call and DynamoDB write, then manually applied the equivalent patch to agent-replay/contracts/vulnerable-dependency.yaml and re-ran `agent-replay gate` with a prompt reproducing the same regression (skip strands-agents-tools) -- PASSed, confirming the patch is correct and sufficient. Reverted the contract file afterward: this was a verification of the patch's correctness, not a real acceptance decision, which stays a human's call per the "no dashboard writes to git directly" design.
2026-09-14 | CC | Phase 1 (real deploy): IAM fixed by the user (AmazonAPIGatewayAdministrator, IAMFullAccess; stuck agent-replay-dashboard-api stack deleted). Confirmed the old stack gone, rebuilt dashboard-api.zip, deployed infra/dashboard-api.yaml for real -- succeeded first try. Real API: https://337k8w3bbi.execute-api.ap-south-1.amazonaws.com. All four endpoints curled against real data and confirmed correct, including a real POST /accept verified by a direct dynamodb get-item (then reverted to the prior genuine value). Pointed the web app at it via web/.env.local (VITE_AGENT_REPLAY_API_URL), fixed a real gap found along the way -- .gitignore only covered bare .env, not .env.local -- before it could leak. Screenshotted the triage view loading from the real deployed API with the URL visible. Found and fixed the one real behavioral difference from the local stand-in: GET /runs/{unknown_id} returned 500 AccessDenied on the real Lambda (its role lacked s3:ListBucket, so S3 won't disclose object non-existence, per S3's own documented behavior) where the local stand-in (running under broad dev credentials) returned a clean 404 -- added a scoped s3:ListBucket (bucket-level resource, s3:prefix condition limited to comparisons/*) rather than papering over it by catching AccessDenied as not-found, per instruction (masking a permissions failure as missing data is the worse failure mode). Separately fixed a real leak: the Lambda's top-level exception handler echoed raw boto3/IAM error text (role ARNs, policy detail) straight to the client in every uncaught-exception path -- now logs full detail to CloudWatch via logging.exception and returns a generic message. Hit a real CloudFormation gotcha while redeploying: it only diffs a Lambda's Code.S3Bucket/S3Key, so reusing the same S3 key meant the IAM policy update landed but the code did not -- forced with aws lambda update-function-code, confirmed via CodeSha256 match. Audited every AWS call the Lambda makes against the deployed role: all five now confirmed working live (GetObject+ListBucket, Query on CommitIndex, Scan, UpdateItem); could not independently confirm CloudWatch log delivery -- my own CLI user lacks logs:DescribeLogStreams, unrelated to the Lambda's own role.
2026-09-14 | CC | Phase 2 PR-readiness audit (no commits/pushes made, per instruction). Grepped everything that would be pushed for AWS account IDs, ARNs with account numbers, absolute paths, and credentials: clean, except one pre-existing, already-committed absolute-path leak in web/src/fixtures/*.ts (hardcoded D:/Project/agent_replay in demo fixture data, from commit 20a2653, predates this task) -- reported, not fixed (fixture data, not live config, low severity, but a real local-path disclosure). Confirmed the 7 curated trace files stage correctly per the hybrid decision. Walked infra/github-oidc.yaml against spike/storage.py's actual boto3 calls the way Phase 1 did for the dashboard-api role, and found the same class of gap: the comment's "1:1 mapping" was accurate when written but predates Phase 2's save_test_run(), which calls Table.put_item() directly -- dynamodb:PutItem was missing, meaning EVERY --storage aws test/gate call in CI (not just recording) would fail persisting dashboard data. Fixed in the template (not deployed -- that stack is the user's to deploy by hand per Phase 2 rules). Found one CI-blocking data gap, not a code bug: only vulnerable-dependency has any --storage aws reference recording in the real account (confirmed via direct DynamoDB lookups, all 5 runs present); customer-refund and vulnerable-dependency-mixed have zero -- `agent-replay test` (no scenario arg, exactly what CI runs) would ERROR on both from a missing reference recording, not from any real regression, making a clean green baseline across all three scenarios currently impossible. Verified path-portability is NOT a problem on Linux (agent_replay/portable.py's {repo} token expands via Path.as_uri()/str(), OS-correct by construction) and pyproject.toml's dependencies are self-contained for a fresh `pip install -e .`. Full walkthrough, remaining decision points, and exact hand-off commands delivered to the user in chat.
