"""agent-replay: record real Strands agent runs, derive a human-reviewable behavioral contract from them,
and gate future runs against it. See README.md.

Byte-identical replay by injection. It does not make the model deterministic.
"""

__version__ = "0.1.0"
